/**
 * The deck library: SPEC §11 R171 and R172, BUILD M9-T1.
 *
 * STUB. The contracts commit wires this module into `allRoutes()` so the route table and the
 * composition root are settled before the server work lands; M9-T1 replaces this body with the
 * `/api/decks` routes and `frozenDeckFor`.
 */

import type { Route } from "./http";

export function createDeckRoutes(): Route[] {
  return [];
}
