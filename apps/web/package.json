{
  "name": "@jackioh/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "// build:e2e": "A production build deletes what the suite drives. main.tsx:168 serves NotFound for /dev/hotseat, net.ts:670 and hotseat.tsx:97 return early, so `window.__jackioh` is absent: measured 0 occurrences of it in a `vite build` bundle and 7 in this one. The two bundles differ by 20 bytes and nothing else — MODE is the only thing this flag moves.",
    "build:e2e": "vite build --mode development",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@jackioh/ai": "workspace:*",
    "@jackioh/cards": "workspace:*",
    "@jackioh/engine": "workspace:*",
    "@jackioh/shared": "workspace:*",
    "@jackioh/validator": "workspace:*",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "7.0.1",
    "@testing-library/react": "16.3.3",
    "@testing-library/user-event": "14.6.7",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "jsdom": "30.1.0",
    "vite": "8.3.0"
  }
}
