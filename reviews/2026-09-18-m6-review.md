# JackiOh review — 2026-09-18 — 7c476c9 (HEAD moved to a30c3fd mid-review; see B-15)
Scope: Part A (re-verification only, see below) | Part B (milestone M6, B6 primarily)
Verdict: **FAIL**

Produced by an independent reviewer session following REVIEW.md, run as the separate session
CLAUDE.md rule 8 requires. No file in the repository was modified except this one.

## What was and was not re-checked

**Part A was not re-run from scratch.** `reviews/2026-09-17-part-a.md`, `-r2`, `-r3` and `-r4` were
read in full. r4's verdict was PASS with 0 BLOCKER, 0 MAJOR, 13 MINOR, against "SPEC.md revision 4".

Re-checked mechanically against SPEC.md **as it stands today**:

| Prior claim | Today | Method |
| --- | --- | --- |
| §11 holds R1–R81, no gaps, no duplicates | §11 holds **R1–R168**, 168 rows, no gaps, no duplicates | regex over `^\| R(\d+) \|` |
| zero dangling § or R references | zero dangling § or R references (53 headings resolve) | every `§n.n` and `R<n>` in the file resolved against headings and §11 rows |
| §8: 100 non-token + 5 token rows | 100 non-token rows (each index once) + 51.1, 65.1, 90.1, 93.1, 95.1 | regex over §8 row heads |
| rarity 35/37/16/7/5 = 100 | Common 35, Rare 37, Epic 16, Legendary 7, Token 5 | recount of the §8 rarity column |
| r4 A-1 (§10.2 "answers any open prompt") | phrase gone — resolved | grep |
| r4 A-3 (§5.1 "never its identity until it fires") | phrase gone — resolved | grep |
| r4 A-6 (header carries no revision) | header reads "revision 5 of 2026-09-17" — resolved | SPEC.md:3 |
| r4 A-7 (auto-end has no §11 row) | "auto-end" still occurs once, outside §11 — **unresolved** | grep |

**Not re-checked, and this is the gap:** A1 (completeness against the three source files), A1.3's
card-by-card diff, A2.4–A2.9, A3.1–A3.6, and every one of R82–R168. Part A has never been run
against SPEC revision 5. REVIEW.md line 3 says "Run A … again whenever SPEC.md changes"; SPEC.md has
since more than doubled its ruling count (commits `a1185e1`, `673453e`, `ef9cbeb`). Recorded as
**B-9**.

## Findings

| ID | Severity | Location (file:line or SPEC §) | Expected (SPEC §) | Observed | Evidence |
| --- | --- | --- | --- | --- | --- |
| B-1 | BLOCKER | `apps/server/test/sql/02_rls_as_client.sql:5–34` | §9.1 "Read a projection" of **its own** row; §9.8 "Reading deck order or the opponent's hand → never sent" | The two per-profile sections are nine bare `select count(*)`s under comments saying "must be 1" / "must be 0". Nothing compares them; the runner's `FAIL\|UNEXPECTED` grep has nothing to catch. A real cross-account leak passes the gate | Mutation `alter policy loadout_deck_cards_select_own … using (true)` applied after the migrations: profile 2 read profile 1's deck card — the file printed `deck_cards_visible │ 1` directly under `-- loadout_deck_cards: must be 0` — and the suite exited **0**. Same result for `loadouts_select_own` and `collection_grants_select_own` |
| B-2 | BLOCKER | `apps/server/test/sql/03_match_lifecycle.sql` (37 top-level `select`s, **0** `raise exception`) | B6: nonce dedupe; §9.5 reaper and "both tickets are claimed in one atomic statement"; §9.3 append-only log | Almost the whole file prints values and asserts nothing. Three mutations each left `pnpm test:sql` at exit 0: (a) `app.append_match_action` → `select -1::bigint` — the action log records nothing and nonce dedupe is gone; the file printed `seq_1_again │ -1` under `-- the same nonce must return the original seq, not a new row` and `logged_actions │ 0`; (b) `app.reap_stale_matches()` → `select 0` — the ceiling reaper stops working; (c) `app.claim_ticket_pair(...)` → `select true` — a second matcher claims the same pair | the three mutations, applied to a throwaway Postgres after the four migrations; harness is a copy of `run.sh` with its own container. `03:108–114`, `:174–175`, `:177–183` |
| B-3 | BLOCKER | `packages/engine/test/rulings.test.ts:962–972`; `03_match_lifecycle.sql:233–255`; `apps/server/src/api/codes.ts:136–145, 289–307` | §9.4 "A global circuit breaker disables redemption and alerts when system-wide failures cross a threshold in a window" | R106's only behavioural proof is the `### R106 … ###` heading in 03, which cannot fail. Deleting the switch (`delete from app.settings where key = 'redemption_enabled'`) left the gate **green** while the section printed `breaker_open │ ok`, `uses │ 1` and `status │ active` — the opposite of every comment under it. `rulings.test.ts`'s own R106 body only greps two constants and two seeded settings rows out of source text. The breaker that **is** tested (`codes.test.ts:589–675`) is a different, in-process `BreakerState` closure; nothing in `apps/server/src` reads `redemption_enabled` (grep returns 0 hits outside the migration) | mutation m12; `grep -rn "redemption_enabled" apps packages --include=*.ts` → migration only |
| B-4 | BLOCKER | `03_match_lifecycle.sql:24, 40, 49, 75, 97, 137, 165` | A check must prove the specific failure, not any failure | All seven `raise notice 'FAIL …'` probes in 03 are guarded by `exception when others then raise notice 'OK …'`. This is the pattern the 2026-09-18 audit reported as fixed; it was fixed in 01 only | Replacing `app.save_loadout` with a body that does nothing but `raise exception 'boom'`: 03 printed `OK (L2/L1): boom: save_loadout is broken`, `OK (L4/L2): boom…` and `OK: boom…` and the suite exited **0**. L1/L2/L4 and R105 at the SQL level are therefore unproved |
| B-5 | BLOCKER | `apps/server/src/index.ts:45–84`; `apps/server/src/db/` | §9.2 topology `F[API functions] --> P[(Postgres)]`; §9.4 "Redemption is one server-side transaction"; "Every collection change writes `collection` and `collection_grants` in one transaction" | There is **no production `Store`**. `apps/server/src/db/store.ts` does not exist; `loadStore` dynamically imports `./db/store` and throws `StoreUnavailableError`. The server boots only in `E2E=1` mode, on the in-memory `e2e-store.ts`. §9.4's transaction semantics exist twice and are joined nowhere: once in TypeScript against a port with no implementation, once as `app.*` SQL functions that no TypeScript calls. Every `apps/server` vitest assertion about a database property is an assertion about `test/fakes/store.ts` | `ls apps/server/src/db` → `migrate.ts`, `migrations/`, `seed-catalog.ts`. `grep -rn "db/store"` → only `index.ts`. Acknowledged in `index.ts:45–52` ("which the database agent owns"), but not in BUILD.md, not in a §11 ruling, and not in the M6 gate record |
| B-6 | MAJOR | `apps/server/test/api/results.test.ts`; `apps/server/src/api/results.ts:8`; `0004_matches.sql:360` | B6: "results for every terminal reason"; §2.5's seven endings | `both-heroes-dead` is produced by the engine (`packages/engine/src/stateCheck.ts:79`), named in `results.ts`'s own header as one of the seven and allowed by the `results.reason` CHECK constraint — and **no test in `apps/server` names it**. The six tested are hero-death, concede, draw-accepted, turn-cap, disconnect, match-ceiling | `grep -rn "both-heroes-dead" apps/server` → 2 hits, both non-test |
| B-7 | MAJOR | `apps/server/test/api/queue.test.ts`; `apps/server/test/api/loadouts.test.ts:388–405` | §9.4 "Decks are frozen into the queue ticket"; §9.8 "Deck swapped after matchmaking → decks are frozen into the ticket" | The only freeze test is `deckFor` returning a copy, i.e. that an array is not aliased. No test saves a different loadout **after** enqueue and asserts the paired match still uses the ticket's deck — which is the abuse vector §9.8 names. `store.loadouts.replace` appears in `queue.test.ts` only inside the fixture helper | `grep -n "loadouts.replace" apps/server/test/api/queue.test.ts` → line 53 only |
| B-8 | MAJOR | `apps/server/test/fakes/engine.ts:1–19, 276`; `apps/server/test/match/actor.test.ts:329`; `apps/server/test/match/recovery.test.ts:25` | BUILD M6-T4: "the opponent's socket never receives the other hand's `defId`s (protocol-level test)"; "killing the actor mid-game and reconnecting yields the same `viewFor`" | Both run against `createFakeEngine`, whose `viewFor` is written by the test suite and hard-codes the redaction (and `hero.health: 30`, `mana: 4/4`). No test wires the real engine into an actor: `engine.real.test.ts` only proves the adapter builds, registers the catalog and reduces one play. The fake's justifying comment — "`packages/engine` is mid-build (M3 in flight: `viewFor`, `prompts`, `traps` and `combat` do not exist yet)" — has been stale since M3 closed | `fakes/engine.ts:4`; `engine.real.test.ts:72–95` |
| B-9 | MAJOR | REVIEW.md:3; SPEC.md:3 | "Run A … again whenever SPEC.md changes" | The last Part A ran against revision 4 with §11 at R1–R81. SPEC.md is now revision 5 with R1–R168. R82–R168 — which include every server ruling M6 rests on (R104–R112, R137, R142–R149, R156–R168) — have never been through a Part A audit | 4 Part A reports, none later than 2026-09-17 14:24; `grep -c '^| R[0-9]* |' SPEC.md` = 168 |
| B-10 | MINOR | `apps/server/test/api/collection.test.ts:49–100`; `apps/server/test/fakes/store.ts:106–122` | M6-T2: "a grant with `reason` writes both tables or neither (fault-injection test)" | The fault injection is real and tested both ways round, but it exercises the fake store's snapshot-and-restore `tx`, not a database transaction. There is no SQL-level fault-injection counterpart. Mitigated, not covered: `app.grant_cards` is one plpgsql function (atomic by construction) and CHECK 10 asserts the two tables agree after a launch grant | the fake `tx` clones and restores `tables` on throw |
| B-11 | MINOR | `01_schema_invariants.sql:139–156` | a check that could not fail is a finding | CHECK 4 has no vacuity guard. Verified as declared rather than hidden: `public` holds **0** functions, so the check is a regression trap, not a measurement | `select count(*) … pg_proc … nspname='public'` → 0 |
| B-12 | MINOR | `01_schema_invariants.sql:1`; `run.sh:79–82` | — | `\set ON_ERROR_STOP on` makes 01 report one defect per run; every check after the first failure is skipped, and because 03 depends on the state 01 leaves, a single 01 failure also reddens 03 for an unrelated reason. Observed in four of the seven mutations | mutations m1–m4 each aborted 01 at the first `raise` and then 03 |
| B-13 | MINOR | `apps/server/test/api/codes.test.ts:531–587`; `src/api/http.ts:460` | §9.4 "identical time" | The test runs on a virtual clock (declared) and asserts `max(work) < REDEMPTION_RESPONSE_FLOOR_MS` (250 ms). `padTo` is a floor, so it is a no-op once real work exceeds it; nothing tests or bounds that regime, and nothing ties 250 ms to a measured database latency | `padTo(timers, startedAt, floorMs)` = `sleep(floorMs - elapsed)` |
| B-14 | MINOR | `apps/server/src/api/codes.ts:136–145, 340–344` | §9.4 "A **global** circuit breaker" | `openUntil` lives in a per-`createCodesRoutes()` closure. The failure *count* is store-wide, so each process opens at the same threshold, but the open state itself does not survive a restart and is not shared. `codes.test.ts:651` asserts this behaviour rather than flagging it | the file's own comment: "Deliberately a value, not module state" |
| B-15 | MINOR | `7c476c9..a30c3fd` | a review names one commit | This review was taken at `7c476c9`. Another session committed twice while it ran, moving HEAD to `a30c3fd`. Nothing M6 moved: the diff is `06-room-code.cy.ts` (**comment-only** — every `+`/`-` line outside the diff header is a comment, so the M6 gate spec is unchanged in substance), `09-deckbuilder.cy.ts` (M8) and `reduce.test.ts` (a timeout). No server source, no migration, no SQL check file and no SPEC.md change. Every finding above stands at `a30c3fd` | `git diff --stat 7c476c9..a30c3fd` |

## Coverage of checks — B6, item by item

| B6 item | Result | Evidence |
| --- | --- | --- |
| Invite redemption: each of the six transaction steps has a test | **pass** | `codes.test.ts` carries one `describe` per step with the step quoted: step 1 `:108` (3 `it`s), steps 2+3 `:159` (4), step 4 `:241` (3), step 5 `:294` (6), step 6 `:388` (3). The order matches `codes.ts`, including §9.4's consequence that a rate-limited attempt is *not* logged (`:258`). Server + validator projects green: 17 files, 260 tests, 1.9 s |
| Failure responses identical **in body** | **pass** | `codes.test.ts:433` compares the whole serialized body across missing / expired / exhausted and pins the exact bytes `{"error":{"code":"invalid_code","message":…}}`, plus a single status set. A revoked and a malformed code take the same path (`codes.ts:256–272`) |
| Failure responses **within 5 ms** over 50 samples | **pass, with B-13** | `codes.test.ts:532`: 150 interleaved samples, fresh profile and IP each, all asserted to be the step-5 refusal; anti-vacuity control asserts the *injected* work is lopsided by > 5 ms and that the floor is what flattens it. Virtual clock (declared); floor-regime only |
| Circuit breaker | **FAIL — B-3** | The SQL proof cannot fail (mutation m12 green). The TS proof at `codes.test.ts:589–675` is genuine but tests a second, unrelated breaker. Nothing connects the two |
| Collection: no client-writable path | **pass** | `collection.test.ts:265` asserts the route table is exactly `["GET /api/collection"]` with `auth: "active"`, and `:275` drives POST/PUT/PATCH/DELETE through the real router expecting 404/405 with both tables still empty. At the SQL level CHECK 5 and CHECK 6 hold and both **bite**: `create policy … for update to authenticated` → `FAIL (CHECK 5): write policy reachable by a client role` |
| Grants and collection written atomically | **pass at the fake layer, B-10 / B-5 below it** | `collection.test.ts:49`, `:66` (fault on either write, both orders) and `:83` (mirror control requiring a pre-existing row to roll back). Measured against `fakes/store.ts`'s `tx`, which is a re-implementation |
| Validator is a single module imported by web and server | **pass** | `apps/server/test/validator-single-source.test.ts`: real `import … from "@jackioh/validator"` statements anchored to line starts (so a comment cannot pass), manifest dependency per app, no app declaring its own `validateLoadout` / `DECK_SIZE` / `MAX_COPIES` / `LOADOUT_DECKS`, and no second copy of an L1–L6 sentence — with the sentence fragments extracted from `packages/validator/src/index.ts` at test time. It carries its own anti-vacuity test |
| Unique index enforced at SQL level | **pass** | CHECK 3 and CHECK 14 both bite, proved independently. Re-keying the index on `(profile_id, slot, card_id)` → `FAIL (CHECK 3): … with slot in the key it only repeats the primary key and L4 is unenforced`. Dropping the index and running CHECK 14's block alone → `FAIL (CHECK 14): L4 index allowed the same card in two decks`, psql exit 3 |
| Match actor: crash-and-fold | **pass at the fake layer — B-8** | `recovery.test.ts:114` deep-equals the full view for both seats across a `registry.stop`, drives the real `src/match/clock.ts`, compares `hashState` and proves the nonce map was rebuilt from the log — all against `createFakeEngine` |
| Match actor: nonce dedupe | **pass in TS, FAIL in SQL — B-2** | `actor.test.ts:303` is real (fake store enforces single-shot). The SQL half (`03:101–114`) prints three seq numbers and a row count and asserts none of them; `append_match_action` reduced to `select -1` leaves the gate green |
| Match actor: per-player view | **pass at the protocol layer — B-8** | `actor.test.ts:329` walks real serialized frames key by key with disjoint decks and both hands asserted non-empty first. The redaction it checks is the fake's own |
| Decks frozen into the ticket | **FAIL — B-7** | Only `deckFor` returning a copy (`loadouts.test.ts:401`). The swap-after-enqueue vector is untested |
| Clock and grace | **pass** | `clock.test.ts` — 14 `it`s: turn clock expiry naming the active player, reset on turn change, R79's 30 s off-turn prompt clock pausing the turn clock (and the control that an *active*-player prompt does not), per-player grace, R147's non-extending flapping socket, cancel on return, clock still running while disconnected, ceiling measured from `startedAt`, snapshot/remaining, stop-on-over, idempotent `stop()` |
| Reaper | **pass in TS, FAIL in SQL — B-2** | `results.test.ts:222–261`: resolves past-ceiling as a draw with ratings unchanged (R112), leaves a live match alone, no-ops after the actor, keeps its own row. The SQL half (`03:177–183`) asserts nothing; `reap_stale_matches → select 0` stays green |
| Results for every terminal reason | **FAIL — B-6** | 6 of the 7 covered; `both-heroes-dead` has no test |

## Coverage of checks — the rest of Part B that bears on M6

| Check | Result | Evidence |
| --- | --- | --- |
| B0 setup | **not run, by assignment** | The full suite, fuzz, coverage and Cypress were excluded (another agent is driving CI and a third the browser suite). Ran instead: `vitest run --project server --project validator` → 17 files, 260 tests, 0 failed; `pnpm test:sql` → exit 0, ~4 s; `pnpm rulings:coverage` → exit 0 |
| B4 rulings index | **pass as an index** | `pnpm rulings:coverage`: "168 rows, R1–R168 / named by a test: 168 / every row is named, and no test names a row that does not exist." It is a naming check: four of the five rulings whose named proof is a `03_match_lifecycle.sql` heading (R106, R110, R111, R112) rest on checks that cannot fail — see B-2, B-3. R104 is the exception and is sound (it catches `check_violation` only, so any other error propagates and stops psql) |
| SQL gate exists and runs | **pass** | `.github/workflows/ci.yml` job `sql` runs `sh apps/server/test/sql/run.sh` on its own runner with a pre-pulled `postgres:16`. Note the workflow has never executed on a real runner (no remote) — carried forward from the prior gate record, not re-checked here |
| `01_schema_invariants.sql` is able to fail | **pass — verified independently, not taken on trust** | 61 `FAIL (CHECK n)` messages behind 63 `raise exception`s across 17 checks. Five mutations, each applied after the four migrations to a throwaway database, each turned the suite red with the right message: drop `app.assert_catalog_version` → CHECK 15 (the historic false pass); re-key the L4 index → CHECK 3; add an UPDATE policy for `authenticated` → CHECK 5; `disable trigger all` on `collection_grants` → CHECK 7; `reset search_path` on `app.save_loadout` → CHECK 17. The vacuity guards in CHECKs 1, 5, 6, 11, 12 and 14 are real guards |
| The two declared limitations | **one honest, one understated** | CHECK 4's no-vacuity-guard claim is exactly true (`public` holds 0 functions) — B-11. The claim that 02 and 03 "still report by `raise notice`, caught only by the runner's output grep" **understates them**: 02 has 0 `raise exception` and 6 notice-FAIL paths against 13 printed selects, 03 has 0 and 7 against 37. The majority of both files has no FAIL path of any kind, so the grep has nothing to grep — B-1, B-2, B-4 |

## Not verified

- **The full vitest suite, the fuzz gate, the coverage run and Cypress.** Excluded by assignment;
  another agent holds CI and a third the browser suite. The figures carried forward unverified are:
  whole suite 3,062 across 214 files, lint 0, typecheck 0, e2e 33/33 on two browsers.
- **Part A against SPEC revision 5.** See B-9 and the table at the top: only the mechanical
  invariants (ruling numbering, cross-references, §8 counts, rarity recount) were re-checked.
  R82–R168 have never been audited.
- **Any GitHub Actions run.** There is no remote; the `sql` job is verified only as a file that
  invokes a script this session ran locally.
- **Wall-clock behaviour of the invite gate.** The 5 ms test is a virtual clock (B-13).
- **The real engine under the match actor.** Nothing in the repository does this (B-8); the
  redaction itself is covered by `packages/engine/test/viewFor.test.ts`, which was not read here.
- **`e2e/06` passing.** Not run. Its uncommitted diff was read and is comment-only (B-15).
- **The `app.*` SQL functions' bodies.** Audited for the invariants 01 asserts and for the mutations
  above; not line-read against §9.4 clause by clause.
- **Whether `02` and `03` were ever intended as gates.** Both are wired into `run.sh`'s pass/fail
  loop and into the CI `sql` job, and five §11 rulings cite `03` as their proof, so this report
  treats them as gates. If they are meant as diagnostics, the rulings index is what needs fixing.

Verdict: **FAIL.**
BLOCKER ids: **B-1, B-2, B-3, B-4, B-5.**
MAJOR ids: B-6, B-7, B-8, B-9.
