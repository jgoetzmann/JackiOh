# JackiOh review — 2026-09-18 — 7c476c9
Scope: Part A (status re-check only, see below) | Part B (milestone M5)
Verdict: FAIL-MAJOR

Produced by an independent reviewer session following REVIEW.md, per CLAUDE.md rule 8. Nothing in
the repository was modified except this file. Both builds measured below were written to a
scratchpad directory, never to `apps/web/dist`, so the concurrent browser suite was not disturbed.

## What was and was not run

Under the operating constraints given for this session:

- **Part A was not re-run from scratch.** The four prior runs (`reviews/2026-09-17-part-a.md`,
  `-r2`, `-r3`, `-r4`) were read, and their findings were re-checked against today's SPEC.md by
  targeted grep plus one mechanical recount. Details and the exact re-check list are in the
  Part A section.
- **Not run: `pnpm test` (whole suite), `pnpm fuzz`, `pnpm test:coverage`, `pnpm test:e2e`.**
  Another agent is driving CI and a third the browser suite. Everything they cover is marked
  "not verified" below rather than restated from their reports as if measured here.
- **Run here:** `eslint .` (exit 0, no output); `tsc -p tsconfig.json` (exit 0);
  `tsc -p apps/web/tsconfig.json` (exit 0); six targeted vitest files; two real Vite builds.
  `pnpm typecheck` itself was *not* run, because its first step is
  `pnpm --filter @jackioh/cards run gen`, which rewrites a source file. The two `tsc` invocations
  above are its two M5-relevant halves, run without the generator.

---

## Findings

| ID | Severity | Location (file:line or SPEC §) | Expected (SPEC §/BUILD) | Observed | Evidence |
| --- | --- | --- | --- | --- | --- |
| B-1 | MAJOR | `apps/web/src/game/animations.ts:39,305-310`; `apps/web/src/game/Hero.tsx:7-13` | BUILD M5-T4 `modifierChanged`: "Player modifier badge appears or fades by the hero", acceptance "badge list equals the view's modifiers" | The badge does not exist. `animTestid.modifiers` resolves `modifiers-<side>`, and **no component renders that testid**; `SideView`/`HeroView` carries no modifier list at all. So the row animates an element that is never in the DOM, the acceptance is unimplemented and untested, and §8 #77 (Professor Curvature) and #78 (/fullsend) are invisible to the player while active. `animations.test.ts:515` ("resolves a target for every event type without throwing") passes because it only checks that a *string* comes back — never that the element exists | `grep -rn "modifiers" apps/web/src/game/{Board,Hero,Game,Hand}.tsx` → only the three comment lines in `Hero.tsx`, which say so outright: "nothing carries the player modifiers … adding `modifiers: {id,label}[]` to `SideView` is the fix". `grep -rn "modifiers-" apps/web/src e2e` → `animations.ts`, `e2e/support/testids.ts:180` and `e2e/README.md` only; no renderer, no assertion |
| B-2 | MAJOR | `apps/web/src/game/Board.test.tsx:88-127`; `e2e/cypress.config.ts:19-22` | BUILD M5-T1 acceptance: "a snapshot test renders a fixture `PlayerView` … **without layout overflow at 1280×720 and 390×844**" | Measured by nothing, in either place, and the two places point at each other. `Board.test.tsx:88` states that jsdom has no layout engine and that the pixel check "is the Cypress spec's job (BUILD M8)"; `e2e/cypress.config.ts:19-20` states that "the responsive case (390x844) is a component test there". No Cypress spec calls `cy.viewport` or asserts `scrollWidth`. The substitute static scan has partial teeth but reads **only `board.css`** and only literal `width:`/`min-width:` px values — `prompt.css`, `index.css` and `animations.css` are not scanned, and `max-width`, `flex-basis`, `grid-template-columns` px and `min-inline-size` are not matched | `grep -rn "390\|844\|1280\|720\|viewport\|scrollWidth" e2e/cypress/ e2e/support/ e2e/cypress.config.ts` → four hits, all inside `cypress.config.ts`, none an assertion. `Board.test.tsx:124` scans `boardCss` only (`readFileSync(… "board.css")` at :29) |
| B-3 | MINOR | `apps/web/src/game/Game.tsx:230-237`; `e2e/support/testids.ts:73` | BUILD M5-T4 `turnStarted` and `turnAutoEnded`, acceptance "banner text" | The banner is rendered but asserted nowhere. `testid.banner` / `turn-banner` / the `BANNER` constant appear in no vitest test and in none of the twelve Cypress specs | `grep -rn "turn-banner\|testid.banner\|BANNER" apps/web/src e2e` → `animations.ts` (the resolver), `Game.tsx` (the renderer), `e2e/support/testids.ts` (the unused constant), `e2e/README.md`. Zero assertions |
| B-4 | MINOR | `apps/web/src/game/animations.ts:629-630,696-706` | BUILD M5-T4 fixes a duration per event; SPEC §10.10 asks for a table so "each animation has an acceptance criterion" | `BURST_BUDGET_MS = 2400` and `MIN_ENTRY_MS = 120` rescale a whole backlog proportionally, so the duration actually played can be shorter than the table's. The file argues the case well, and the tests pin both behaviours, but no BUILD item and no §11 ruling sanctions the departure (CLAUDE.md rule 3 would want a row if it counts as a decision the spec does not make) | `animations.ts:696` `fitBudget()`; `animations.test.ts:679` "plays a burst too long for the budget proportionally faster" |
| B-5 | MINOR | `apps/web/src/game/animations.test.ts:213`; `packages/shared/test/events.test.ts` | BUILD M5-T4: "exactly one row per event type **in SPEC §10.3**" | No test reads SPEC §10.3. The chain is SPEC → (by hand) `GAME_EVENT_TYPES` → `ANIMATIONS`; the number 41 is hand-copied into two tests. An event added to §10.3 alone would change nothing in `vitest run` or `tsc`. Currently correct: I diffed all four lists mechanically and they agree as sets, 41 each | My diff: SPEC §10.3 = 41; `GAME_EVENT_TYPES` = 41, identical **and in the same order**; `ANIMATIONS` keys = 41, same set; BUILD M5-T4 table = 41, same set; symmetric difference empty in both directions |
| B-6 | MINOR | `apps/web/src/game/animations.ts:39`, `:41` | Cross-references resolve (A2.1 in spirit) | `animTestid.modifiers` is documented as "§6.4"; SPEC §6 has only 6.1, 6.2 and 6.3, so the citation is dangling. `animTestid.backrow` is declared and used by no row in the table | `grep -n "^### 6\." SPEC.md` → 6.1, 6.2, 6.3. `grep -n "animTestid.backrow" apps/web/src/game/*.ts*` → no hits |
| B-7 | MINOR | `apps/web/src/game/Prompt.test.tsx:373-395` | REVIEW.md B5: "Every prompt kind has a component test that **submits** the correct `answer`" | Five of ten kinds (`zone`, `tribute`, `direction`, `x`, `embiggen`) have a component test that submits a correct **`play`** action (R81) and, for the case where the engine *does* open them as a real `PendingChoice`, only a render assertion — the submit path is never exercised in a component test. The mapping itself is unit-tested for all ten in `actions.test.ts:540`. Narrow, and §10.6 says those prompts are unreachable for Core cards | `Prompt.test.tsx:383` `it(\`${kind} keeps its picker and its data-prompt-kind\`)` asserts `kindOfModal()` and `getByTestId(present)` only; `onAction` is created and never asserted on |
| B-8 | MINOR | `apps/web/package.json` (the `"// build:e2e"` note) | Evidence in the repo is accurate | The note's central claim is exactly right (0 vs 7 occurrences — I reproduced it). Its second claim, "The two bundles differ by 20 bytes and nothing else", is not: the hotseat chunk differs by **712** bytes (7292 → 8004) and the match chunk by 97 (11410 → 11507); only the index chunk differs by ~20 (228315 → 228334) | `stat` on both scratchpad builds, per-file, with content hashes stripped from the names |
| A-P1 | MAJOR | SPEC.md (HEAD ef9cbeb, 09-18 09:04) vs `reviews/2026-09-17-part-a-r4.md` | REVIEW.md Part A: "Run A before any build work and **again whenever SPEC.md changes**" | SPEC.md has changed substantially since the last Part A run. r4 audited a §11 holding **R1–R81**; §11 today holds **R1–R168**. The 87 rulings R82–R168, and the §2–§10 edits that came with them, have never been through Part A. A fresh Part A was out of scope for this session by assignment, so this is reported, not closed | r4 line 7: "§11 holds R1–R81 with no gaps"; my recount of today's SPEC: "ruling rows: 168 unique: 168 min: 1 max: 168". `git log -- SPEC.md`: four commits after r4's timestamp |
| A-P2 | MINOR | SPEC.md:723 (§11 intro) | A2.1/§1: the revision note describes §11 | The note reads "The spec reviews of 2026-09-17 added R58 to R90: … and R83–R90 in the milestone reviews of the same day". §11 holds R1–R168; R91–R168 are unaccounted for in the note. This is r4's A-6 (revision identity) widened rather than closed | SPEC.md:723, quoted phrase "added R58 to R90"; recount above |

No BLOCKER was found. B-1 is the closest: it is a missing element rather than a wrong rule, which
is why it is MAJOR under REVIEW.md's severity definitions and not FAIL.

---

## Part A — what was re-checked, and what was not

### Re-checked, mechanically, against today's SPEC.md

A throwaway script (scratchpad, not committed) recounted the spec. Results:

| Property | Value today | r4's value | Status |
| --- | --- | --- | --- |
| §11 ruling rows | 168, unique, R1–R168, **no gaps, no duplicates** | R1–R81 | grew; see A-P1 |
| `R<n>` references with no §11 row | none | none | still clean |
| `§n` references with no heading | none | none | still clean |
| §8 non-token card rows | 100, indices 1–100 each exactly once | 100 | unchanged |
| §8 token rows | 5 (51.1, 65.1, 90.1, 93.1, 95.1) | 5 | unchanged |
| §7 token rows | 9 | 9 | unchanged |
| Rarity recount | Common 35, Rare 37, Epic 16, Legendary 7 = **100** | 35/37/16/7/5 | unchanged |
| "decide" rows | R1, R2, R4, R5, R14, R26, R39, each a named constant in `packages/engine/src/config.ts` | same | still true |

(The recount's "duplicate indices 1–8" are §9.8's build-order table rows, and its sixth "Token"
rarity cell is §7's header row — both checked by hand at SPEC.md:546-553 and :332.)

### Re-checked by targeted grep: prior BLOCKERs and MAJORs

| Prior finding | Status today | Evidence |
| --- | --- | --- |
| r1 A-1 (BLOCKER, Transmogulate pool) | **still fixed** | SPEC.md:464 "pool = the non-token cards whose rarity above is Legendary, except #83: #52, #85, #87, #92, #93, #95"; mirrored in R35 at :761 |
| r1 A-2 (BLOCKER, cast-on-draw chain has no terminator) | **still fixed** | R58 at :784, `CAST_ON_DRAW_CHAIN_CAP` = 20 |
| r3 A-1 (MAJOR, R77 ingredient fate) | still fixed | R77 at :803 |
| r3 A-2 (MAJOR, Immutable Fuse target) | still fixed | R61 at :787 |
| r3 A-3 (MAJOR, #26/#52 choice path) | still fixed | R81 at :807 |
| r3 A-4 (MAJOR, bounced Heroic Power) | still fixed | R43 at :769 |
| r3 A-5 (MAJOR, clock during an off-turn prompt) | still fixed | R79 at :805 |
| r4 A-1 (MINOR, §10.2 timeout vs R79) | **now closed** | SPEC.md:648 now matches R79 word for word |
| r4 A-3 (MINOR, §5.1 owner-keyed trap visibility) | **now closed** | the sentence "never its identity until it fires" no longer occurs in SPEC.md |
| r4 A-7 (MINOR, auto-end rule has no §11 row) | **now closed** | SPEC.md:80 "the turn auto-ends (R82)" |
| r4 A-10 (MINOR, #90 does not cite R80) | **now closed** | SPEC.md:470 "a full library refuses the shuffle (R80)" |

### Not re-checked (and therefore not claimed)

- A1.1, A1.2, A1.4, A1.5 — the source-document mappings. Not re-walked; r4 passed them and no
  source file has changed (`JackiOh_Mechanics.md`, `JackiOh_Core_Cards.md`, `ARCHITECTURE-CCG.md`
  are all still 2026-09-17 00:48).
- A1.3's clause-by-clause read of 105 card rows. Not redone. Only the mechanical counts above.
- A2.4, A2.6, A2.7, A2.8, A2.9 and all of A3 — not re-walked.
- **R82–R168 have had no Part A pass of any kind**, from anyone. That is A-P1.
- r4's remaining MINORs A-2, A-4, A-5, A-6, A-8, A-9, A-11, A-12, A-13 — not re-checked
  individually.

---

## Part B — B5, line by line

REVIEW.md B5 has five bullets. Each is answered on its own line.

**1. "`animations.test.ts` passes and every `GameEvent["type"]` has a row."** — **PASS, and the
guard was proved able to fail.**

`vitest run --project web src/game/animations.test.ts src/game/Prompt.test.tsx
src/game/actions.test.ts` → 3 files, **120 tests, all passed**, 2.68 s.
`ANIMATIONS` holds 41 keys; `GAME_EVENT_TYPES` holds 41; `GameEvent["type"]` has 41 members; SPEC
§10.3 names 41; BUILD M5-T4's table has 41 rows. All four agree as sets (diffed, symmetric
difference empty both ways); `GAME_EVENT_TYPES` also matches §10.3 in order.

The exhaustiveness guards are three deep and I proved two of them bite, against the real types,
without touching the repo — a scratchpad file importing `GameEventType` from
`packages/shared/src/events.ts`:

- `const INCOMPLETE: { [K in GameEventType]: Row } = { cardPlayed: … }` →
  `error TS2740: … is missing the following properties … cardResolved, summoned, damage,
  healthLost, and 36 more.` This is the exact shape `ANIMATIONS` is declared with
  (`animations.ts:143`), so a forgotten row is a compile error.
- `NoneMissing<Exclude<GameEventType, (typeof SHORT)[number]>>` with a short list →
  `error TS2344: Type '"cardResolved" | … ' does not satisfy the constraint 'never'.` This is the
  exact shape of `GameEventTypesAreExhaustive` (`events.ts:163`).

Both guards are live in CI's typecheck: `tsc -p tsconfig.json` includes `packages/*/src`
(so `events.ts`), and `tsc -p apps/web/tsconfig.json` includes `src` (so `animations.ts`). I ran
both: **exit 0, zero errors.**

The third guard is `packages/shared/test/events.test.ts`, which re-derives the union from the
source text of `events.ts` and compares — a genuine independent witness, not a restatement of the
array, with its own anti-vacuity assertion (`expect(declared.length).toBeGreaterThan(0)`).
`vitest run --project shared` → 1 file, 3 tests, passed.

Caveat recorded as B-5: nothing reads SPEC §10.3 itself.

**2. "Every prompt kind has a component test that submits the correct `answer`."** — **PASS with a
qualification (B-7).**

All ten §10.6 kinds have a component test in `Prompt.test.tsx`, each rendering the real component,
firing real clicks, and asserting on `onAction`:

| Kind | Test | What it asserts is submitted |
| --- | --- | --- |
| discover | `:56` | `{type:"answer", selection:[{pick:"mode", option:"<defId>"}]}` |
| target | `:81` | an `answer` with the instance / hero selection |
| mode | `:106` | `answer` with the option string, prefix stripped |
| mulligan | `:128` | `{type:"mulligan", keep:[…]}` in hand order |
| hand | `:154` | `answer` with the chosen instance |
| zone | `:182` | `{type:"play", …, zone:{row:"units", lane:5}}` (R81) |
| tribute | `:215` | `play` with `tributes:["u1","u2"]`, gated by min/max |
| direction | `:248` | `play` with `modes:["left"]` |
| x | `:272`, `:298` | `play` with `x:2` / `x:1` |
| embiggen | `:311` | `play` with `embiggen:true` |

Mirrored by ten `answerAction` cases in `actions.test.ts:540` ("one case per PromptKind (§10.6)").
The qualification is B-7: for the last five, the *prompt* submit path (as opposed to the *play*
path) is rendered but never submitted in a component test.

**3. "The client never enforces rules: see B1.5."** — **PASS.**

`grep -rn "canAttack\|isLegal" apps/web/src` returns ten hits, all of one helper:
`Card.tsx:25 isLegal(highlight, testId) → highlight.legal.has(testId)` — a set membership test, not
a rule. The set is built only by `actions.ts:433 highlightFor(view, legal, interaction)`, which
reads `ActionBody[]` returned by `legalActions` plus the open prompt's own `options` and nothing
else; I read all ~110 lines of it. The client imports the engine in exactly one non-test file
(`engine.real.ts`, behind an unanalyzed dynamic import). `Card.tsx:35` `data-legal` "is the only
gate on firing `onClick`", and `Board.test.tsx:196` plus five cases at `actions.test.ts:161` pin
that an illegal click produces no action.

Spot-checks of the neighbouring invariants, run here: B1.1
`grep -rn "Math.random\|Date.now\|new Date\|fetch(\|from \"fs\|setTimeout" packages/engine/src
packages/cards/src` → four hits, **all inside comments** saying the thing is banned; zero real
hits. B1.7 `grep -rn "state.players\|\.hand\.push\|\.graveyard\.push" packages/cards/src` → two
hits, **both comments**. `eslint .` → exit 0, no output.

**4. "Reduced-motion drains a full game's events synchronously."** — **PASS.**

`animations.test.ts:600` builds `longStream(6)` — **240 events, every type six times** — enqueues
it under `setReducedMotion(true)` with an injected clock, and asserts
`expect(clock.schedule).not.toHaveBeenCalled()` plus `pending()===0`, `inFlight()===null`,
`animating().size===0`, `idle()===true`, `onSettled` once. The mechanism is real, not stubbed:
`durationFor` returns 0 under reduced motion (`animations.ts:476`), and `pump()`'s loop
(`:712-730`) `continue`s past every zero-duration entry without scheduling. A second test at `:621`
proves the same when reduced motion is read from the media query rather than passed in. The CSS
half is pinned at `:236` and the `--anim-scale: 0` under `prefers-reduced-motion` exists at
`apps/web/src/index.css:36`.

**5. "Hotseat route exposes `window.__jackioh` only outside production builds (`grep` the build
output for `__jackioh`)."** — **PASS. Measured, this session, on two real builds.**

Both builds were written to a scratchpad `--outDir` so `apps/web/dist` was never touched.

| Build | Command | `__jackioh` occurrences in the whole output |
| --- | --- | --- |
| production | `vite build` (default mode) | **0** |
| e2e | `vite build --mode development` (= `pnpm build:e2e`) | **7** |

Both exit 0. The 7 split as: `assets/hotseat-*.js` — 3 × `__jackioh` + 1 × `__jackiohE2E`
(`routes/dev/hotseat.tsx:287`), `assets/match-*.js` — 3 × `__jackioh` (`net.ts:672`, the second
publisher, which the check must cover and does). `pnpm build` and `pnpm build:e2e` therefore differ
as expected.

The zero is not vacuous — the production build still ships the route:
`dist-prod/assets/hotseat-BCl5RMum.js` is 7292 bytes and still contains the string
`Hand over to`, so the handle was dead-code-eliminated rather than the chunk dropped. The e2e copy
is 8004 bytes.

Two guards back this at the source level, and I read both: `main.tsx:168` serves `NotFound` for
`/dev/hotseat` under the same `MODE !== "production"` condition, and
`apps/web/src/routes/dev/hotseat.test.tsx:406` stubs `MODE=production`, re-imports the module and
asserts the handle is absent — carrying its own anti-vacuity line,
`expect(import.meta.env.MODE, "vi.stubEnv must reach import.meta.env, or this proves nothing")`.
`vitest run --project web src/game/Board.test.tsx src/game/hotseat.test.ts
src/routes/dev/hotseat.test.tsx` → 3 files, **62 tests, all passed**.

---

## Part B — the rest of M5

### BUILD M5-T1 … M5-T4 acceptance items

| Item | Result | Evidence |
| --- | --- | --- |
| M5-T1 snapshot of a fixture with 10 units, 10 backrow, a stacked pile | pass | `Board.test.tsx:62` (`toMatchSnapshot`, snapshot committed at `apps/web/src/game/__snapshots__/Board.test.tsx.snap`, 1750 lines, tracked by git) and `:67`, which asserts the fixture's own contents and that all 20 slots draw a `.card`/`.card-back` |
| M5-T1 no layout overflow at 1280×720 and 390×844 | **fail — B-2** | measured nowhere; see the finding |
| M5-T1 opponent hand = `count` backs, no names | pass | `Board.test.tsx:131`, asserts back count equals `count`, `hand-count-opponent` text, and `back.textContent === ""` for every back |
| M5-T2 a component test per prompt kind | pass, with B-7 | table above |
| M5-T2 an illegal click produces no action | pass | `Board.test.tsx:196`; `actions.test.ts:161` |
| M5-T2 opponent seat shows "waiting for choice", no options | pass | `Prompt.test.tsx:402`, asserts the text, `optionTestids()` empty, no submit button, `onAction` never called |
| M5-T3 `window.__jackioh` gated on MODE | pass | above |
| M5-T3 same seed + actions → same hash in the browser **and in vitest** | pass (vitest half verified here) | `packages/cards/test/hotseat-replay.test.ts` folds a committed browser recording with the real `reduce`, the real catalog and the 109 scripts against a **written-down** `EXPECTED_HASH = "b2a3f515"`, plus `EXPECTED_RESULT` and `EXPECTED_ACTIONS` as second anchors. `vitest run --project cards test/hotseat-replay.test.ts` → 6 tests passed. The older `hotseat.test.ts:223` is now explicitly disclaimed in a comment as *not* the M5-T3 acceptance. Browser half: not verified (Cypress not run) |
| M5-T4 exactly one row per event type | pass | above; guards proved able to fail |
| M5-T4 reduced motion → 0, drains synchronously | pass | above |
| M5-T4 per-row acceptance column (41 rows) | **partial** | sampled and measured: `.damage-pop` and `.heal-pop` asserted in `04-combat.cy.ts`; `.loss-pop` and `data-locked` in web tests; `rotate(90deg)` in both; `result-overlay` in `01-hotseat-full-game.cy.ts`; `data-prompt-kind` in four specs; `--lunge-distance ≥ 20px` at `animations.test.ts:245`. **Not asserted anywhere: `turn-banner` (B-3) and `modifiers-<side>` (B-1).** The rest of the column was not exhaustively swept |

`data-animating="<eventType>"` is genuinely implemented (`Card.tsx:206`, `Game.tsx:129` in a
*layout* effect so it lands before paint, `animations.css:18` `[data-animating]`), and `cy.settled()`
drains it (`e2e/support/commands.ts:865`), so the suite waits on it rather than on a sleep.

### M5 gate — "`e2e/01` and `e2e/02` pass against the hotseat route"

**Not verified here.** Cypress was excluded by assignment. What I could check statically:

- Both specs exist. `02-prompts.cy.ts` has exactly **ten** `it`s, one per picker kind
  (`mulligan, zone, discover, target, hand, mode, tribute, direction, x, embiggen`), and each pairs
  the picker assertion with a board effect that only a `reduce` that accepted the action can
  produce.
- `grep -rn "cy.wait([0-9]" e2e/` → **nothing** (B7's house rule holds).
- `01-hotseat-full-game.cy.ts:211` compares the browser's own `hashState` with a Node fold via
  `cy.task("replayHash")` — two states, one hash function.
- CI runs both jobs against `pnpm build:e2e` + `vite preview`, on Chrome and Electron
  (`.github/workflows/ci.yml`). Whether CI has ever gone green on a runner is **not verified**; the
  HEAD commit message ("CI could not have run at all: pnpm 11.3 refuses Node 22.12, which .nvmrc
  pinned") says the previous state was that it could not, and `.nvmrc` now reads 24.19.0.

### Working tree

`git status --porcelain` at the time of review: `M e2e/cypress/e2e/06-room-code.cy.ts`,
`M e2e/cypress/e2e/09-deckbuilder.cy.ts`, `?? reviews/2026-09-18-m5-m6-m8-gates.md`. Neither
modified spec is M5's. This review reads HEAD (7c476c9) plus that working tree.

---

## Coverage of checks

| Check | Result | Evidence |
| --- | --- | --- |
| A1.1, A1.2, A1.4, A1.5 | not re-run | r4 passed them; no source document has changed since |
| A1.3 | partial | mechanical counts only (100 indices once each, 5 token rows); no clause re-read |
| A2.1 | pass | 0 dangling `§` refs, 0 dangling `R` refs, 168 rows with no gaps or duplicates |
| A2.2 | pass | 100 + 9; rarity 35/37/16/7/5 = 100, recounted from the tables |
| A2.3 | partial | "decide" rows are exactly R1, R2, R4, R5, R14, R26, R39 and all seven are constants in `config.ts`; the full Ruling-marker sweep was not redone |
| A2.4–A2.9, A3.1–A3.6 | not re-run | r4 passed them; **not valid for R82–R168** (A-P1) |
| B0 | partial | `eslint .` exit 0, 0 findings; `tsc -p tsconfig.json` exit 0; `tsc -p apps/web/tsconfig.json` exit 0. `pnpm test`, `pnpm test:coverage`, `pnpm test:e2e`, `pnpm fuzz`: **not run** |
| B1.1 | pass (spot check) | grep clean, comments only |
| B1.5 | pass | `isLegal` is set membership; `highlightFor` reads only `legalActions` + prompt options |
| B1.7 | pass (spot check) | grep clean, comments only |
| B5 bullet 1 | pass | 120 tests green; 41 = 41 = 41 = 41; both compile guards proved able to fail |
| B5 bullet 2 | pass (B-7) | ten component tests, each asserting the submitted action |
| B5 bullet 3 | pass | see B1.5 |
| B5 bullet 4 | pass | 240-event stream, `schedule` never called |
| B5 bullet 5 | **pass, measured** | 0 occurrences in `vite build`, 7 in `vite build --mode development` |
| B2, B3, B4, B6, B7, B8 | out of scope / not run | M5 review; the suites they depend on were excluded |

---

## Not verified

1. **The full test suite, the fuzz gate, the coverage floor and Cypress.** Excluded by assignment
   (two other agents are running them). Every count quoted from them elsewhere in this repo is
   theirs, not mine. What I ran is six targeted files: web `animations` + `Prompt` + `actions`
   (120 passed), web `Board` + `hotseat` + `routes/dev/hotseat` (62 passed), shared (3 passed),
   cards `hotseat-replay` (6 passed).
2. **`pnpm typecheck` as a whole.** Its first step regenerates a source file. I ran its two
   M5-relevant `tsc` projects instead; both exit 0. The engine, cards, validator and server
   projects were not typechecked here.
3. **The M5 gate itself** (`e2e/01` and `e2e/02` green against the hotseat route). Static reading
   only.
4. **Whether CI has ever completed on a runner.** No runner access.
5. **Actual pixel layout at 1280×720 and 390×844.** Nobody has measured it — that is B-2, not a
   limitation of this session.
6. **The `EXPECTED_HASH` in `hotseat-replay.test.ts` as "the browser's hash".** It is a real,
   written-down constant and the fold reproduces it here; that it equals what the browser computes
   rests on spec 01, which I did not run.
7. **Part A for R82–R168** — see A-P1. Nothing in this report should be read as a Part A pass on
   today's SPEC.md.
8. **The remaining rows of BUILD M5-T4's per-row acceptance column.** I measured a 15-artifact
   sample, not all 41.
9. **r4's MINORs A-2, A-4, A-5, A-6, A-8, A-9, A-11, A-12, A-13** — not individually re-checked.

---

Verdict: **FAIL-MAJOR**.
BLOCKER ids: **none**.
MAJOR ids: **B-1, B-2, A-P1**.
