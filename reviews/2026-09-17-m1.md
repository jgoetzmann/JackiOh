# JackiOh review — 2026-09-17 — M1 (uncommitted working copy)
Scope: Part A | Part B (milestone M1)
Verdict: PASS (after one fix round)

Produced by an independent reviewer session following REVIEW.md, run twice: a first audit of the M1 working copy, then a re-verification after the fixes. Condensed here; the reviewer's findings and evidence are kept verbatim where they matter.

## First run — FAIL
1 BLOCKER, 3 MAJOR, 22 MINOR. Commands: `pnpm lint` exit 1, `pnpm typecheck` exit 0, `pnpm test` exit 0 (12 files, 93 tests).

| ID | Severity | Location | Observed |
| --- | --- | --- | --- |
| B-1 | BLOCKER | packages/engine/src/replay.ts:21 | `pnpm lint` exits 1 on `@typescript-eslint/no-unused-vars` for the `_`-prefixed rest sibling in `const { applied: _applied, ...rest } = state`. REVIEW §B0: any non-zero exit is a BLOCKER on its own |
| B-2 | MAJOR | packages/engine/src/mana.ts:56–80 | No test calls `effectiveCost`, so R65's whole order (override → costMod → discounts → Curvature at 4 → floor 0) is unverified; `mana.ts` 75% lines with the discount and Curvature loops uncovered |
| B-3 | MAJOR | packages/engine/test/draw.test.ts | BUILD M1-T7's "a cast-on-draw card drawn into a full hand is still cast (R58)" has no test, though the code is right |
| B-4 | MAJOR | packages/engine/test/setup.test.ts:98 | The R43 test keeps the whole hand, so "including a copy the mulligan returned to the library" is never exercised |
| B-5 … B-23 | MINOR | various | 200 action probes rather than 200 states (B-5); an undocumented 64-entry dedupe window (B-6); a `nextTurnOf` modifier dropped at its own turn's cleanup (B-7); `ringNeighbor`'s third parameter missing from BUILD (B-8); no fill-board helper (B-9); `ceilingReached` absent from BUILD M1-T3's list (B-10); a literal `2 * 40` guard (B-11); `KEYWORD_KINDS` mislabelled as R21's pool (B-12); `pnpm test -- --coverage` not enabling coverage (B-13); a Reborn unit emitting `enteredGraveyard` before leaving again (B-14); an override on an X-cost card not making it free (B-15); a fatigue `drawn` event with empty ids (B-16); `cardPlayed` and `summoned` both emitted for one play (B-17); `legalActions` listing unreachable `answer` actions (B-18); `expireModifiers` iterating the active player twice (B-19); the anti-oneshot cap split between a card flag and config (B-20); R62, R65, R68 and R70 implemented without a test named after them (B-21); BUILD and REVIEW still saying R1–R81 (B-22); no test for a timeout on an off-turn prompt, which M1 cannot produce (B-23) |
| A-1 … A-8 | MINOR | SPEC.md | Header revision number, the §11 intro's credit, §6.3's Fuse row, R77's run-on sentence, §10.9's Script list missing `cost` and `startOfGame`, R82 versus R36's draw offer, the rarity criteria, #85's cell |

Part A was otherwise clean: 105 source headers and 105 §8 rows, every index once, 0 cost and stat diffs, rarity 35/37/16/7/5, R1–R82 with no gaps, zero dangling references.

## Fixes applied between runs
- **B-1:** `eslint.config.js` configures `no-unused-vars` with `^_` patterns and `ignoreRestSiblings`, leaving the purity ban untouched.
- **B-2:** new `packages/engine/test/mana.test.ts`, 11 tests covering R65 end to end; `mana.ts` reaches 100% lines.
- **B-3, B-4:** the two missing acceptance tests.
- **B-5:** the property test walks 200 distinct states and probes every listed action.
- **B-6 … B-22:** each fixed as the re-verification records below.
- **A real rules bug this audit surfaced:** start-of-turn and end-of-turn hooks were firing for both players' cards. `triggerOrder` now takes a controller filter and both call sites pass the active player (§6.2; #13 "not on the opponent's end"), with the regression test "fires a start-of-turn trigger on its controller's turn only (§6.2)".
- **After the re-verification:** R48's timing (a next-turn discount does nothing on the turn it was created) is enforced in `effectiveCost` through `modifierIsLive`, with two tests; BUILD M1-T8's prose, M5-T4's `summoned` row and the M3 gate carry the notes the reviewer asked for.

## Re-verification — PASS
0 BLOCKER, 0 MAJOR, 5 MINOR. Commands: `pnpm lint` exit 0, `pnpm typecheck` exit 0, `pnpm test` exit 0 (13 files, 111 tests at the time of the re-run, 113 now), `pnpm test:coverage` exit 1 on the 90% definition-of-done floor only (81.38% lines).

The reviewer confirmed the BLOCKER and all three MAJORs fixed, 15 of 17 minors fixed, the trigger-controller fix real and correctly scoped (the unfiltered path stays for M4's end-of-turn trap window, which must fire on both sides), and nothing broken: purity greps still clean, no `any`, the event union still exactly §10.3's 40 types, both M1 gates still running their full 100 seeds.

Remaining findings:

| ID | Severity | Location | Observed |
| --- | --- | --- | --- |
| B-7a | MINOR | packages/engine/src/modifiers.ts:45 | The `nextTurnOf` expiry fix had no test — **since addressed** by "R48 keeps a next-turn modifier through its own turn and drops it after the next one" |
| B-7b | MINOR | packages/engine/src/mana.ts | A `nextTurnOf` discount was live from creation, including the turn Curvature was played, which R48 forbids — **since addressed** by `modifierIsLive` and "R48 a next-turn discount does nothing on the turn it was created" |
| B-17 | MINOR | packages/engine/src/reduce.ts:87, :96 | Playing a permanent emits `cardPlayed` and `summoned` for one card; M5 scope, and BUILD M5-T4's `summoned` row now says the client plays the pair as one motion |
| B-21r | MINOR | packages/engine/test/turn.test.ts | The R62 test proves the start-of-turn half only; the end-of-turn half needs traps and delayed effects, so the M3 gate now names it |
| A-7 | MINOR | SPEC.md:357 | Six Commons carry traits §8's rarity criteria reserve higher (#28, #42, #48, #54, #62, #70). Counts are right; a judgement call for the designer |

## Scope
M2–M8 are out of scope and absent, so B1.6, B3, B5, B6 and B7 are unverified rather than failed: no `viewFor.ts`, `combat.ts`, `triggers.ts`, `traps.ts`, `prompts.ts`, `subsystems/`, `cards/catalog.json`, `apps/web`, `apps/server`, `e2e/`, `rulings.test.ts`, `animations.test.ts` or `fuzz.test.ts`. `reduce` answers "combat arrives with M2" and "hero powers arrive with M3" by design. The coverage shortfall sits almost entirely in code whose milestone has not opened (`damage.ts`, `stateCheck.ts`, `modifiers.ts`, `effects/*`).

**Every M1 acceptance item in BUILD M1-T1 … M1-T8, plus both M1 gates, is backed by a named passing test. The gate is clear to open M2.**
