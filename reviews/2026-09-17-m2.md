# JackiOh review — 2026-09-17 — M2 (uncommitted working copy)
Scope: Part A | Part B (milestone M2)
Verdict: **FAIL-MAJOR**

Audited tree: the working copy as snapshotted at 19:31:33 CDT, which is the tree `pnpm lint`, `pnpm typecheck`, `pnpm test` and `pnpm test:coverage` were run against. The repository has no commits (`git rev-parse HEAD` → "unknown revision"; everything untracked). **The tree changed twice during the audit** — see *Not verified*.

Result: **0 BLOCKER, 3 MAJOR, 23 MINOR.** Part A passes every check; revision 5 clears 12 of the fourth run's 13 MINORs and every new finding is wording, a reading choice, or an omitted rationale. Part B's tooling is green (`lint` 0, `typecheck` 0, `test` 0: 19 files / 180 tests), the M1 and M2 gates both run and pass, and the ten damage steps really are one test per step in order. The three MAJORs are: a Reborn unit returning mid-turn is neither summoning sick nor re-exerted, so one unit attacks twice in a turn (§4.1); `forceAttack` does not apply Stack dormancy (§3.2, R13) on either side of a forced combat; and M2-T3's acceptance item 6 (Fed Fauci's Plague Token per damage instance) has no implementation and no test.

## Findings

| ID | Severity | Location (file:line or SPEC §) | Expected (SPEC §) | Observed | Evidence |
| --- | --- | --- | --- | --- | --- |
| A-1 | MINOR | SPEC.md:357 (§8 rarity paragraph) | A1.3: rarity as the paragraph states it | Counts are exact (recounted 35/37/16/7/5 = 100) but six Commons carry traits the paragraph reserves higher: #28 reads three zones, #42 exiles 8 with a Lucky roll, #48 has a radiant modal choice, #54 sets a cost override, #62 is two primitives, #70 reads two state values. Unchanged carry-forward of the fourth run's A-8 | :357 "Common: keywords only, one primitive with at most one target" |
| A-2 | MINOR | SPEC.md:303 (§6.3) vs JackiOh_Mechanics.md:58 | A1.1: Discover's engine semantics | Source parameterises it ("X is 3 unless otherwise specified"); §6.3 fixes "Choose 1 of 3 options". Nothing in Core is lost — every §8 Discover offers 3 — but a future non-3 Discover has no rule | :303; source :58 |
| A-3 | MINOR | SPEC.md:262, :280, :304 (§6.1–§6.3) | A1.1: unnumbered keyword defaults | The source's "Lucky 1", "Echo 1 … recast once" and "Tribute 1" defaults are not restated, although Armor (:249) and Combo (:279 "X defaults to 1") state theirs. No §8 cell depends on them: Lucky and Tribute are always numbered, and the one unnumbered Echo (#51r) resolves it inline | :249, :279 vs :262, :280, :304 |
| A-4 | MINOR | SPEC §2–§4 vs JackiOh_Mechanics.md:16 | A1.2: each source bullet maps to a sentence | "This damage persists between turns" has no sentence of its own. It is encoded unambiguously and structurally (:120 "current health = max health minus damage", :622 `damage: number`, :51 cleanup expires only "this turn" effects, R78 resets damage only on leaving the field), so no number changed | source :16; :120, :796 |
| A-5 | MINOR | SPEC.md:389 (#22) vs source | A1.3: clause equivalence | Source "Cry: Tribute a Permanent" → §8 "Tribute one of your other permanents". R41 (:759) rules "Cannot eat itself; must eat if able"; the narrowing to *your* permanents has no ruling | :389; :759 |
| A-6 | MINOR | SPEC.md:448 (#74) vs source | A1.3: clause equivalence | Source "heal X life" does not name a target; §8 fixes "heal your hero X". No §11 row records the choice | :448 |
| A-7 | MINOR | SPEC.md:336–340 (§7 Type column) vs §8 | A2.6: §7 and §8 agree on tokens | §7 lists the five indexed tokens' types without the `Token` tag (`Spell, KY`, `Unit`, `Spell, CN`) where §8 carries it (`Spell, KY, Token`). §5 :197 puts `Token` in `tags`, so §8 is authoritative; §7's omission could mislead a catalog author | :336 vs :423-row tags |
| A-8 | MINOR | SPEC.md:353 (§8 intro) | A2.1: cross-references | "(the 4 shared tokens are in section 7)" reads as if §7 held only four rows; §7 holds all nine | :353 vs :328 "Nine token definitions" |
| A-9 | MINOR | ARCHITECTURE-CCG.md:53, :283, :341–343 vs SPEC §9 | A1.5: every section represented | Three rationale passages have no §9 counterpart (0 grep hits): the action log's balance-telemetry benefit, §6.1's "scheduled play window" bullet, and the build-order notes "Step 5 before step 7 is deliberate" / "Steps 1 and 4 are load-bearing". Every *mechanism* is present; only the reasoning is dropped | grep "telemetry\|play window\|load-bearing" SPEC.md → 0 |
| A-10 | MINOR | SPEC.md:398 (#31) | A1.3: clauses live in Base or Radiant | The Fib table is the one source clause that appears only in the Engine column. The Base cell names `Fib(cost+1)`, so nothing is lost | :398 |
| **B-1** | **MAJOR** | `packages/engine/src/stateCheck.ts:140-172`; `zones.ts:195-215, 241` | §4.1 "a unit cannot attack the turn it entered the field"; "Each unit has one exertion per turn: one attack or one position switch" | A Reborn unit re-enters the field with `summonedTurn` deleted and `exertion` cleared, so it is not summoning sick and may act again the same turn. Measured: a unit attacks the hero (30→28), dies, returns through Reborn, and attacks again in the same turn (28→26); and a unit played this turn that dies and returns can attack immediately. §4.5 step 4 and R78 do say the instance returns "reset" (R78 lists `exertion` and `summonedTurn` among the reset fields), so the spec is genuinely ambiguous and two implementers would diverge — but the observed result contradicts §4.1 and M2-T1's acceptance "both resets at the controller's next turn". No §11 row resolves it | probe: `can it attack again this same turn? null` / `p2 hero after second attack: 26`; `after reborn, summonedTurn: undefined refusal: null`. `stateCheck.ts` step 4 never re-establishes entry state after `placeOnField`; `reduce.ts:88` is the only writer of `summonedTurn` |
| **B-2** | **MAJOR** | `packages/engine/src/combat.ts:229-231` | §3.2 / R13 "Cards underneath are dormant: … not 'on the field' for effects, not targetable"; BUILD M2-T2 "a card dormant under a Stack is neither an attacker nor a legal target" | `forceAttack` guards only `zone.z !== "field"`, which is true of a dormant card, so R53's path ignores dormancy in both directions. Measured: a dormant card forced to attack dealt 9 to a bystander and took the 5 back; a dormant card used as a forced target took 4 and struck back for 9, killing the attacker, while the active top card of its own zone took nothing. The validated path is correctly guarded by `isActiveOnField` (combat.ts:42-46, 55, 75) — the fix was not extended to the forced path. Latent today: the only Stack card (#92) and the only forcing cards (#9, #60) arrive in M4 | probe: `A) dormant unit forced to attack: bystander.damage = 9`; `B) dormant unit as forced target: enemyUnder.damage = 4 … enemyTop.damage = 0`; `combat.ts:230` `if (attacker.zone.z !== "field") return;` |
| **B-3** | **MAJOR** | BUILD M2-T3 acceptance 6; `packages/engine/src/damage.ts:110` | "Fed Fauci gains exactly one Plague Token per instance, none on an instance Armor reduced to 0, which also emits no `damage` event (R63)" | The second half is tested; the Plague-Token half has no implementation and no test. `grep -ri plague packages --include=*.ts` finds only type declarations (`state.ts:39`, `events.ts:24`, `view.ts:23`) and a test comment — no counter is ever written, and there is no fixture script for it. damage.ts defers step 6 to the trigger loop, which is M3-T2 (`triggers.ts` absent), so the item is unreachable inside M2's file set. The test names the gap honestly rather than hiding it | `damage.ts:110` "// Step 6 (on-damage triggers) is dispatched by the trigger loop from the `damage` event (§10.3)."; `damage.test.ts:163-166` comment "M1 has no trigger dispatch, so what step 6 makes observable is the `damage` event" |
| B-4 | MINOR | `packages/engine/test/*.test.ts` | BUILD §0 / CLAUDE.md rule 3: "Every ruling has a test whose name starts with its id" | 38 R-rows have a test named after them. Rulings the M1/M2 code implements with no such test: **R82** (`turnAutoEnded`, a named M1-T8 acceptance item — covered by `it("auto-ends a turn when nothing but ending it is left")`; R82 appears nowhere in any test file), **R81** (zone/X/embiggen travel in the `play` action), **R42** (`lastDamagedBy`, set at damage.ts:97 and asserted at damage.test.ts:150), **R51** (cited only in a fixture comment), **R55** (the four `counters`). R1, R26 and R39 are covered only by `it("holds the SPEC §11 'decide' rows at their recommended values")`. `rulings.test.ts` is an M3 deliverable, so this is a naming gap, not missing behaviour | `grep -o 'R[0-9]*' test/*.test.ts` → R1–R80, no R81, no R82 |
| B-5 | MINOR | BUILD M2-T3 acceptance 1; `damage.test.ts:163-180` | "a 0-attack unit's strike-back leaves the shield (R63)" | Proven at the `dealDamage` level (a 0-amount hit leaves the shield) but never through an actual combat; no test puts a 0-attack defender opposite a Divine Shield attacker. The rule holds by construction (`resolveCombat` passes `defenderView.attack` = 0 and `dealDamage` returns at `amountIn <= 0`), so this is a missing composed case | `damage.test.ts:177-179`; `combat.ts:194, 196`; `damage.ts:50` |
| B-6 | MINOR | `packages/engine/src/stateCheck.ts:80-82` | R46: the unit "switches to Attack Position and loses Taunt this turn" | `positionSwitched` is emitted unconditionally, so a marked Indestructible unit already in ATK emits a no-op animation event. The existing test happens to start the unit in DEF | `stateCheck.ts:80` `unit.position = "ATK";` then :82 pushes the event with no guard; `statecheck.test.ts:322` sets `warded.position = "DEF"` |
| B-7 | MINOR | `packages/engine/src/stateCheck.ts:88` | §4.5 step 5 "Repeat until nothing changes" | The loop is capped at 100 passes and returns silently if the cap is hit — no error, no event, no `result`. A sensible guard, but an unstable board would end the check mid-flight with nothing recorded | `stateCheck.ts:88` `for (let pass = 0; pass < 100; pass += 1)` |
| B-8 | MINOR | `packages/engine/src/combat.ts:30` | M2-T2 "each rejection reason has a test" | `AttackRefusal` declares `"not your unit"`, which `whyCannotAttack` never returns (ownership is caught earlier by `reduce.ts:154` with its own message). Nine of the ten refusal reasons are reachable and each has a test; the tenth is dead | `grep -rn "not your unit" packages/` → one hit, the type union |
| B-9 | MINOR | `packages/engine/src/config.ts` (last line); `reduce.ts:262-267` | BUILD §2 constant table; §10.1 `applied: { nonce, events }[]` | `NONCE_HISTORY = 64` is an engine constant not in BUILD §2's table, and it bounds nonce dedupe: the 65th-oldest nonce is forgotten and would be re-applied. §10.1 puts no cap on `applied`. Harmless for a 30-turn match, but undocumented in BUILD | `config.ts` "§9.3: how many answered nonces the state remembers for dedupe"; `reduce.ts:264` |
| B-10 | MINOR | `packages/engine/test/combat.property.test.ts:40-48, 93-142` | BUILD M2 gate wording | The gate passes and the generator is genuinely random (I replicated it: ~6,100 units over 1,000 seeds, every keyword appearing 606–1,303 times, only 4 seeds with a keyword-free board, and `expect(fought).toBe(1000)` prevents a silent skip). Two real limits: (a) the R69 clause the gate wording singles out is **unreachable** — `plain` is 3/3 and `bigBody` 5/10 with buffs ≥ −2, so `maxHealth` never falls to ≤ 0 and the "Indestructible with max health above 0" branch is never taken (it is covered instead by the named R69 test in `statecheck.test.ts:346`); (b) the generator makes no Stack piles and no backrow, so dormancy (B-2) and pipeline steps 2-aura and 3-cap are never randomly exercised. `indestructibleIds` is also computed from post-combat survivors rather than at hit time | `combat.property.test.ts:41` (`plain`/`bigBody` only), :43 `buffs = { attack: rng.int(5) - 2, ... }`, :76-82 |
| B-11 | MINOR | `pnpm test:coverage` | BUILD §5 "Coverage floor: 90% lines in `packages/engine` and `packages/cards`" | 88.13% lines (1010/1146), 84.57% statements, 76.67% branches, 84.05% functions; exit code 1 on the threshold. `engine/src` alone is 90.33% lines; the shortfall is `engine/src/effects` at 54.28% and `prompts.ts` at 0% (M3-T3 code that landed in the tree with no test yet). Per the review assignment this is a definition-of-done item, not an M2 gate item, and is not treated as a failure | coverage table: `modifiers.ts 56.66`, `prompts.ts 0`, `position.ts 0`, `loseHealth.ts 0`; "ERROR: Coverage for lines (88.13%) does not meet global threshold (90%)" |
| B-12 | MINOR | `packages/engine/src/zones.ts:236` | R11: a unit-token card in hand or library "ceases to exist if it leaves that zone other than by being drawn or played, burning included" | The guard fires on `wasOnField`, `from === "resolving"`, or a destination of graveyard/exile. A unit-token card moved hand→library (or library→hand other than by drawing) survives. No M1/M2 caller does this; `shuffleInto` is M3-T1 | `zones.ts:236` |
| B-13 | MINOR | `packages/engine/src/zones.ts:237` | §3.2 / R11 "ceases to exist" | A vanished token is removed from every pile but has its `zone` set to `{ z: "exile" }`, so its own field claims a zone it is not in. `findInstance` no longer returns it and the exile pile length is unaffected, so no count is wrong | `zones.ts:237`; `statecheck.test.ts:457-459` asserts `exile` stays empty |

## Coverage of checks

| Check | Result | Evidence |
| --- | --- | --- |
| A1.1 | pass (MINOR A-2, A-3) | All 57 source Keywords bullets located in §3–§7 with engine semantics (line map built for each). Flicker's absence is deliberate: :324 "no Core card uses it, so the engine has no primitive for it", and grep finds it nowhere in §8, BUILD.md or the source card list |
| A1.2 | pass (MINOR A-4) | All 25 source bullets mapped to §2–§4 sentences; every number present unchanged — 1 mana/turn to 4 (:55), draw 1 (:63), 30-turn cap counting player-turns (:77, :80, R2), auto-end (:80, R82), draw offers 1/turn blocking 3 (:80, R36), P1 3 / P2 4 with the N+2 table (:28), mulligan R9 (:29), 20 cards no duplicates (:84), 30 health (:23), heroes uncapped (:98, :301), one attack or one switch (:125), enter in ATK (:124), no attack on summon turn but may switch (:127), 5 unit zones / 5 backrow / 5 shared lanes (:88, :96-97, :102), self-copy exclusion with the "unless the card names the pool" escape (:211). **No missing or changed number → no BLOCKER** |
| A1.3 | pass (MINOR A-1, A-5, A-6, A-10) | Mechanical diff by script: **105 source headers, 105 §8 rows**, every index (1–100 plus 51.1, 65.1, 90.1, 93.1, 95.1) exactly once, 0 duplicates. **0 cost diffs, 0 base-stat diffs, 0 radiant-stat diffs.** 7 type diffs all named in §5.3 (:236, :233); 5 tag diffs and 5 name diffs are parser artefacts of names containing tag words (#12, #15, #43, #57, #62), re-read by hand as correct; the one real name diff (#90.1) is §5.3 :235. 100 `~~~` separators; the 5 cards with none (#38, #80, #93.1, #95.1, #96) are exactly the "No radiant form" rows; `~~` appears only on #15/#16 as §5.3 :232 predicts. Clause coverage: all 105 rows read side by side plus an integer sweep (1 hit, false positive), a §6-keyword sweep (5 hits, all false positives) and a content-word sweep (26 hits, all synonym artefacts) → **0 missing clauses, 0 wrong numbers, 0 wrong stats**; #24/#74's escaped `\*` multipliers, #95's ten effects, #98's seven powers, #93's six grades and #100's four counters all confirmed |
| A1.4 | pass (MINOR A-7) | §7 has exactly 9 rows (:328 "Nine token definitions"). Lava Golem is card #55 (:428), absent from §7. Shared-token stats match JackiOh_Mechanics.md:42-48 exactly; Bread Token's naming ruling is stated in place. "Generated by" reproduced by grep of §8 for all 9 with no extras or omissions, including Combo-Fodder appearing in #93's **radiant** cell only and CN-Virus's "and itself on each draw" |
| A2.1 | pass (MINOR A-8) | Script check: 53 headings; **zero dangling `§n`, `section n` or `R<n>` references**; §11 holds R1–R82 with no gaps and no duplicates |
| A2.2 | pass | Recounted from the tables, not the sentence: §1 claims 100 + 9; §7 has 9 rows; §8 has 100 non-token rows (105 − 5 `N.1`); the distribution sentence sums to 35+37+16+7+5 = 100 and **equals the recount exactly** (Common 35, Rare 37, Epic 16, Legendary 7, Mythic 5); all 5 token rows carry rarity `Token` |
| A2.3 | pass | 20 `**Ruling` markers, each mapping to a §11 row (the §2.5 auto-end rule that the fourth run flagged now has R82 at :800). **Every "Cards affected" index resolves to a §8 row** (script check, 0 misses). Decide rows are exactly R1, R2, R4, R5, R14, R26, R39 — identical to §11's intro list at :715 |
| A2.4 | pass | §4.4's ten steps are the order every other mention uses: §6.1 Divine Shield step 1 (:256), Armor step 2 (:249), Indestructible step 4 (:259), Poisonous step 7 (:253), Lifesteal step 8 (:254), Trample step 9 (:257), Cleave step 10 combat-only (:258); §8 #44 "Skips pipeline step 2" (:411), #73 "Pipeline step 3" (:447), #84 "Hero armor in pipeline step 2" (:463); R63 (:781) carries both zero rules, the Trample/Lifesteal split and the Cleave guard |
| A2.5 | pass | Recomputed from §8: KY-tagged non-token, non-#57 = {31, 51, 82} = #57's cell (:430); Trap + Field Trap = {18, 41, 60, 71, 85, 96}, **all cost 1** = #67's cell (:441); Legendary non-token = {52, 83, 85, 87, 92, 93, 95}, minus #83 = {52, 85, 87, 92, 93, 95} = R35 (:753) and #83's cell (:462). All three sentences reproduce the recount |
| A2.6 | pass (MINOR A-7) | §3.2 (:114), §6.3 Bounce (:295), §7 (:328, token-rules bullets), R11 (:729), R34 (:752) and R80 (:798) agree on unit tokens vanishing, spell tokens behaving as ordinary cards, and Sheep counting 2 Tributes. The residual is §7's Type column dropping the `Token` tag |
| A2.7 | pass | §5.2's five bullets (:219-223) are restated without contradiction by R74 (:792); the "no radiant form" list at :223 is exactly the 5 §8 rows carrying no `~~~` plus the 4 shared tokens. Row-by-row sweep of every §8 Engine cell mentioning radiant not re-run this round; carried from the fourth run, which passed it |
| A2.8 | pass | R1 is stated once as a rule at :271 (§6.2 Cry) and once as a §11 row at :719; every other mention is a consequence. #3, #12, #22, #61, #69, #81 all read consistently with it. Card-by-card sweep carried from the fourth run |
| A2.9 | pass | The sequence is identical in §2.2 (:37-51), §6.2 (:274-275), §10.3 (:660) and R62 (:780): refresh → start-of-turn delayed → start-of-turn triggers → draw → main → end-of-turn triggers → end-of-turn trap window → end-of-turn delayed → cleanup → cap check. R68 (:786) places delayed effects at their R62 point and the trap-window exception is explicit at :660 |
| A3.1 | pass | Every verb used in a §8 cell has a §6.3 row (:290-322) or a §10.4/§10.7 helper; Fuse is single-sourced in R77 (:795), which revision 5 extended with the no-target type rule, the multi-target radiant #85 case and "every other field of the kept instance is unchanged, `statsOverride` and the Vanilla flag included" — closing the fourth run's A-2 and A-5 |
| A3.2 | pass | Every card choice maps to a §10.6 kind or to a play-action field per R81 (:799). Revision 5's §10.6 (:688) now states which five kinds are play choices and that `hand` is reachable, closing the fourth run's A-4; R81's "Cards affected" now lists #17, #30, #48, #59, #84, #87, #88, closing its A-9 |
| A3.3 | pass | Every random effect names its pool and exclusions or defers to `catalog.query` (:213); chance effects state their probability and whether Lucky applies (R32 :750, R60 :778) |
| A3.4 | pass | Every §8 trigger names its moment; the off-turn ones (traps, #18, #71, #89, #9) name whose turn, and R79 (:797) defines the clock for a prompt held off-turn — with §9.5 (:537) now carrying the 30 s prompt clock and `ceilingReached`, closing the fourth run's A-1 and A-12 |
| A3.5 | pass | Every remembering card names its §10.1 home; R78 (:796) gives each field a lifetime and R43 (:761) closes the bounced/reset Heroic Power hole |
| A3.6 | pass | Terminators stated for every self-recursive effect: R28's chain cap, R58's per-draw cap with R80 and the turn cap, R1 for #12, the base-card Reborn chain for #3r, grade S for #93, R41 for #22, and #85 never matching itself (R61) |
| B0 | pass with one non-zero exit | `pnpm lint` → **0**. `pnpm typecheck` → **0**. `pnpm test` → **0**, 19 files / 180 tests passed, 8.02 s. `pnpm test:coverage` → **1**, on the 90% line threshold only, all 180 tests green (see B-11; treated as a definition-of-done miss per the assignment, not a failure). `pnpm test:e2e` and `pnpm fuzz` do not exist in `package.json` — M5/M8 and M4, out of scope |
| B1.1 | pass | `grep -rn "Math.random\|Date.now\|new Date\|fetch(\|require(\"fs\|from \"fs\|setTimeout" packages/engine/src packages/cards/src` → **zero hits**. `eslint.config.js` carries the ban (`no-restricted-properties` on Math.random/Date.now, `no-restricted-syntax` on `new Date`/`Date()`, plus `no-restricted-globals` for timers, fetch, process, crypto and `no-restricted-imports` for node builtins across `src/**`), and `lint-ban.test.ts` proves it with six tests that lint real fixture files — including "does not ban the clock outside the pure packages" |
| B1.2 | pass | `rng.ts` is a stateless `valueAt(seedInt, n)` stepped by a cursor, so `(seed, cursor)` reproduces a sequence anywhere. `rngCursor` lives in `GameState` (`state.ts:153`) and `reduce` restores and writes it back (`reduce.ts:298, 306`). `rng.test.ts` "resumes from a serialized cursor in another process" runs `test/fixtures/rng-child.ts` in a child process and compares draws. `replay.test.ts` "folds 100 recorded games to the same state hash" — 100 logs, identical `hashState`, and a negative control ("a different seed gives a different hash") |
| B1.3 | pass | `grep -rn "Promise\|async \|await \|callback" packages/engine/src` → **zero hits**; `cloneState` is a JSON round-trip, which enforces the no-closure invariant structurally. `PendingChoice.resume` is declared as serializable data (`state.ts:89-98` `Resume = { defId, hook, step, radiant, instanceId?, data }`, "A serializable continuation … never a closure"). The M3-T3 test that exercises it is out of scope |
| B1.4 | pass (MINOR B-9) | `reduce.ts:270-271` returns the remembered events for a repeated nonce without re-applying; `reduce.test.ts` "dedupes a repeated nonce: same state, no duplicate events". Bounded by `NONCE_HISTORY = 64` |
| B1.5 | pass | `reduce` returns `{ state, events: [], error }` with the **original** state object on every rejection path (`reduce.ts:271-301`), so it is deep-equal by construction. `reduce.test.ts` "rejects an action from the non-active player and leaves the state untouched"; "refuses anything but an answer while a prompt is open"; "refuses an action once the game is over"; "errors on a card that is not in hand and on a zone that is taken"; `combat-validation.test.ts` "surfaces each refusal as the error of an attack action". `PROMPT_OPEN_ACTION_TYPES` and `NON_ACTIVE_ACTION_TYPES` match BUILD M1-T3's two lists exactly. No `apps/` exists, so the client half is out of scope |
| B1.6 | **out of scope** | `viewFor` is M3-T6; `src/viewFor.ts` is absent and `packages/shared/src/view.ts` is types only. M6-T4's protocol test is M6 |
| B1.7 | n/a — out of scope | `packages/cards/src/index.ts` is `export {}` and `packages/cards/test/` is empty; M4 has not started. The grep returns nothing because there is nothing to grep |
| B2 §2.1 Setup | pass | `setup.test.ts`: "deals 3 and 4 from shuffled libraries and opens the first mulligan"; "puts every Quickdraw card in the opening hand and draws that many fewer (§6.2)"; "draws no random cards when Quickdraw already fills the opening hand"; "R9: replacements are drawn before the returned cards are shuffled back"; "R43: Heroic Power rolls its power during setup, deterministically from the seed"; "R43 rolls a power for a Heroic Power the mulligan returned to the library"; "moves on to the opponent's mulligan, then starts turn 1 with a draw (R10)" |
| B2 §2.2 Turn loop | pass | `turn.test.ts`: "R62 runs a turn in order: refresh, start triggers, draw, then end of turn"; "fires start-of-turn triggers before the draw (#37 Gravedigger)"; "clears a this-turn modifier at cleanup and keeps a pending Echo (R30)"; "R68 resolves two end-of-turn triggers on one side in lane order"; "fires a start-of-turn trigger on its controller's turn only (§6.2)". The end-of-turn trap window is M3/M4 and the code leaves the slot with a comment (`turn.ts:111-115`) |
| B2 §2.3 Mana | pass | `turn.test.ts`: "refreshes 1 mana on turn 1, 4 by the fourth turn and stays at 4"; "Mana Well adds temporary mana above the refresh (#6)"; "Hinder's modifier lowers the next refresh and mana never goes below 0"; "refuses an X above current mana and allows X = 0". `mana.test.ts` has eleven `effectiveCost` tests covering costMod, override, player discounts by type, Curvature last and only at 4 (R48), X ignoring modifiers, embiggen read at base price, and Heroic Power's X (R43) |
| B2 §2.4 Draw | pass | `draw.test.ts`: cast-on-draw chain ("casts a drawn Hinder …", "CN-Virus deals 1 through the pipeline, shuffles 2 copies and draws again", "stops a cast-on-draw chain at the cap and leaves the next card in hand (R58)", "R58 casts a cast-on-draw card even when the hand is full"); fatigue ("deals 1, 2 then 3 fatigue damage on three empty draws (R3)"); hand cap ("burns the eleventh card to the graveyard while still counting the draw (R4)"); Infinite Reserves (#75); R80 library cap, three tests |
| B2 §2.5 End | pass (MINOR B-4) | `endgame.test.ts`: "a hero at 0 loses"; "R59: one effect that leaves both heroes at 0 is a draw"; "the same two hits as separate actions end the game at the first one"; "a conceding player loses"; "ends in a draw after the 30th player-turn, not the 29th (R2)"; "an accepted draw offer ends the game as a draw"; "a declined offer blocks the offering player for three of their turns, not the opponent (R36)"; "auto-ends a turn when nothing but ending it is left" (R82's behaviour, unnamed — B-4); "the match ceiling ends the game in a draw (R79)"; "a timeout answers the open prompt and only ends the turn of the player who ran out (R79)"; "disconnect expiry is a loss (R79)" |
| B2 §2.6 Deckbuilding | pass | `validateDeck` (`state.ts:214-232`) rejects size ≠ 20 (§2.6 L2), duplicate ids (L3), Token-tagged defs (L3) and unknown ids (§9.4 L6), each with the rule in the message. `state.test.ts` has one test per rejection, each asserting the named rule. M6-T3's shared validator is out of scope (`packages/validator/src/index.ts` is `export {}`) |
| B2 §3 Zones | pass | `zones.test.ts`, 15 tests: adjacency (lane 1 one neighbour, lane 3 two, never across sides); the two rings, both directions, inverse, backrow independent (R14); full row / locked zone / lock persisting after the occupant leaves; a Reborn-reserved zone is not open (R64); Stack piles (pushed card is top, only it active, the card beneath resumes with its damage, occupied zone refused without Stack); tokens leaving (R11); R78 reset keeping `costMod` and `radiant`; R12 owner's zone under another controller; fill-board. R13's dormancy is proven in `combat-validation.test.ts` ("R13 refuses an attack by a card dormant under a Stack", "R13 refuses a dormant card under a Stack as a target too") — but not on the forced path (**B-2**). R15 is M3-T1 |
| B2 §4.1–§4.2 | pass (MINOR B-1 affects §4.1) | `combat-positions.test.ts`, 10 tests: enter in ATK; R6 switched-then-attack and attacked-then-switch both refused; **"resets both exertions at the controller's next turn, not the opponent's"**; R49 Deft Duelist both ways round; R20 spell switches spend no exertion; DEF grants Taunt; DEF Armor +1 stacking with printed Armor and Big D-fender's aura; Spikey Pillow refused DEF; a sick unit may still switch. `combat-validation.test.ts`, 12 tests: every reachable refusal reason (R6, R7, sickness, Can't attack, R13, not-an-enemy, Rush-vs-hero with Charge allowed, Taunt printed/granted/DEF-position, R5 lane 1 → lane 5), `attackTargets` exactness, the same refusals surfaced through the action layer, and a property test "never lists an attack that whyCannotAttack refuses, and lists every one it allows". B-1 is the exception: the Reborn return bypasses the exertion-reset rule this row asserts |
| B2 §4.4 Pipeline | **pass — ten tests, one per step, in order** | `damage.test.ts` declares them in file order as "step 1" … "step 10", matching §4.4 and BUILD M2-T3: 1 Divine Shield (negates 10, gone, next hit lands, no `damage` event); 2 Armor 7 → 0, DEF +1, Big D-fender +2 more, True Strike ignores all; 3 hero cap 12 → 5, radiant 3, hero only; 4 Indestructible takes 0, stays, **no `damage` event**; 5 the event carries the amount dealt (unit and hero); 6 the zero rules (see **B-3** for the Plague-Token half); 7 Poisonous on 1 dealt, not on 0, never a hero; 8 Lifesteal post-Armor, nothing when Armor zeroes the hit; 9 Trample excess only, non-combat too, and Trample+Lifesteal healing 3+7=10 once; 10 Cleave both neighbours, never across sides, through a Divine Shield. Plus three `heal`/`loseHealth` tests (§6.3, R18, R19). The code's step order matches one-for-one (`damage.ts:52, 62, 72, 78, 83, 110, 112, 122, 127`; step 10 in `combat.ts:122-137`) |
| B2 §4.5 State check | pass (MAJOR B-1; MINOR B-6, B-7) | `statecheck.test.ts`, 13 tests: R68 six deaths from one effect firing six Death triggers, active side first then lane order, run for **both** active players; R8/R64 Reborn reserving its zone while a Death-trigger summon lands elsewhere, and Death firing on both deaths; R47 Locked-zone fizzle; R46 Indestructible mark → ATK, no Taunt, restored next turn; R69 both branches; R59 whole-effect timing asserted on **event indices** (both hits before the first `destroyed`, both Death triggers after it); R78 reset return at 1 health with no Cry; R11 token vanishing; R59 both-heroes draw and single loss. **Verified bug fix 2**: "§4.5 step 1 collects a backrow card an effect marked destroyed" (`stateCheck.ts:28-39, 96`) plus "R46 leaves an Indestructible Field Spell where it is and drops the mark". **Verified bug fix 3**: "R78 a Death trigger reads what the unit remembered before it left the field" asserts `eater.memory` is `{}` while the hook still summons the remembered def — the snapshot at `stateCheck.ts:106-109, 136-138`. **Verified fizzled-Reborn event**: "R47 says so in the event stream when a Reborn return fizzles into a Locked zone" (`stateCheck.ts:150-155`), paired with the suppressed `enteredGraveyard` on the way in (`stateCheck.ts:120-129`) |
| B2 §5 Catalog | out of scope | `cards/catalog.json` and `catalog.test.ts` are M4-T1 |
| B2 §5.2 Radiant | partial, in scope only as the layer swap | `layers.ts:21-29` reads `def.radiant` when `instance.radiant`, keeping `damage` and `buffs`; exercised by `damage.test.ts` step 3 (a radiant Anti-oneshot Armor capping at 3). R22 and R57's named tests are M3/M4 |
| B2 §6 Keywords | partial — M3-T1 | `ls packages/engine/src/effects` gives 9 of the ~34 verbs M3-T1 requires (targets, damage, loseHealth, draw, addToHand, shuffleInto, mana, memory, position). Keyword-set computation per §10.4 is in `layers.ts` and exercised through the combat and damage tests (`grantedKeywords`, auras, position grants, `vanilla`, `divineShieldSpent`, `rebornSpent`, `tauntSuppressedTurn`); layer 2 (Felinor Fiender) is explicitly deferred at `layers.ts:66`. M3-T4's own tests are out of scope |
| B2 §6.2 Triggers | out of scope | `triggers.ts`, `traps.ts` are M3-T2. `turn.ts:21-32` implements R68's side/lane order for start- and end-of-turn hooks and is tested; hand and graveyard triggers arrive with M3 |
| B2 §7 Tokens | partial | Unit tokens vanishing on leaving the field is tested in `zones.test.ts` and `statecheck.test.ts` (R11); spell tokens reaching the graveyard is tested. Stat overrides are wired (`layers.ts:25-26` reads `statsOverride`) but no card sets one yet; fill-board returns zones (`zones.ts:274`). The BUILD "T" row is M4 |
| B3 | out of scope | M4. `packages/cards/src/index.ts` is `export {}`, `packages/cards/test/` is empty, `catalog.json` and `scripts/missing-tests.ts` are absent |
| B4 | pass with gaps (MINOR B-4) | 38 R-rows carry a test named after them: R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R18, R19, R20, R21, R25, R30, R36, R43, R46, R47, R48, R49, R53, R58, R59, R62, R63, R64, R65, R68, R69, R70, R78, R79, R80. **All seven "decide" rows are named constants in `config.ts` with the spec's values** — `CRY_ON_PLAY_ONLY true` (R1), `TURN_CAP_PLAYER_TURNS 30` (R2), `HAND_CAP 10` (R4), `LANE_RESTRICTED_ATTACKS false` (R5), `ROTATION_RING "two-rings"` (R14), `GENN_GREED_EXILES "odd"` (R26), `FIENDER_STATS_MODE "printed-plus-sum"` (R39) — and `config.test.ts` asserts the whole BUILD §2 table plus the decide rows. **No ruling exists only in a code comment**: every `R<n>` cited anywhere under `packages/engine/src` is ≤ R81 and resolves to a §11 row. `rulings.test.ts` is an M3 deliverable and is correctly absent. Rows the M1/M2 code depends on with no test named after them: **R82, R81, R42, R51, R55**, plus R1/R26/R39 covered only by the unnamed decide-row test |
| B5 | out of scope | M5. No `apps/web`, no `animations.ts`, no `animations.test.ts`. Recorded as a positive: `shared/src/events.ts` declares **exactly 40** event types and the set matches SPEC §10.3's 40 names one-for-one, so M5-T4's table has a stable target |
| B6 | out of scope | M6–M7. No `apps/server`; `packages/validator/src/index.ts` is `export {}` |
| B7 | out of scope | M8. No `e2e/`, no `test:e2e` script |
| B8 | pass (MINOR B-10) | `replay.test.ts` folds 100 recorded random-policy games to identical `hashState` values with zero rejected actions, plus a different-seed negative control. `hotseat.smoke.test.ts` plays 100 seeded games to completion (4.5 s), asserting the reason is one of hero-death / both-heroes-dead / turn-cap, `turn ≤ 30`, `phase === "over"`, hands ≤ 10 and no stray empty piles. The M2 gate `combat.property.test.ts` runs 1,000 random combats in 557 ms, asserts all three gate invariants and guards itself with `expect(fought).toBe(1000)`. **Judgement: it meets BUILD's M2 gate wording and is a real property test** — I replicated its generator and measured every keyword appearing 606–1,303 times over the 1,000 boards, with only 4 keyword-free boards. **It is thinner than the wording implies in one specific way worth stating plainly: the R69 branch the gate wording names ("unless it is Indestructible with max health above 0") can never be reached, because no fixture unit's max health can fall to 0 or less.** That clause is covered instead by the named R69 test in `statecheck.test.ts`, so the gate is met — but not by this test. Folding logs in a fresh process is covered for the RNG (`rng-child.ts`) but the fuzz artefacts B8 asks for are M4 |

## Not verified

- **The working copy changed twice while I was auditing it.** I snapshotted the tree at 19:31:33 and audited that snapshot, which is the same tree the four commands ran against. A first wave (19:30–19:34) added `engine/src/prompts.ts` and `effects/choose.ts` and edited `catalog.ts` and `effects/index.ts`; a second wave added `engine/src/triggers.ts` and `test/prompts.test.ts` and edited `reduce.ts` (`import { settle }`, `settle(sink, 0)`), `state.ts` (`QueuedTrigger` gaining `triggerId`/`event`/`isTrap`) and `turn.ts` (`triggerOrder` delegating to `cardsInTriggerOrder`). These are M3-T2 and M3-T3 changes. **No M1 or M2 file changed** (verified by checksum diff), so every Part B finding above stands against the live tree, but the M2 gate was not run on a frozen tree and `pnpm test` did not include the `prompts.test.ts` that now exists. M3 work appears to have opened before this M2 gate closed, which CLAUDE.md rule 2 and BUILD §0 forbid.
- No commits exist, so there is no commit hash to pin the audit to; the title records the working copy instead.
- `pnpm test:e2e` and `pnpm fuzz` are not defined in `package.json`, so B0's last two commands and checks B5, B6, B7 and B8's fuzz-artefact bullet could not be run. B1.6, B1.7, B2's §5/§6.2/§7 rows and B3 have no code to audit. All recorded as out of scope, not as failures, per the assignment.
- A2.7 and A2.8's row-by-row sweeps of every §8 Engine cell are carried forward from the fourth Part A run rather than re-executed; I verified their rule statements (§5.2's five bullets against R74, R1 at :271 against :719) and spot-checked the cards each names.
- B-10's keyword-frequency numbers come from a script that replicates `combat.property.test.ts`'s generator; because my probe consumes the RNG in a slightly different order than the test, they are distributional estimates, not an exact reproduction of that test's boards. The reachability claim (max health can never fall to ≤ 0) is not statistical — it follows from `plain` 3/3, `bigBody` 5/10 and `buffs.health = rng.int(5) - 2 ≥ -2`.
- B-1 and B-2 were confirmed by running throwaway `tsx` probes that import the engine directly; their output is quoted in the evidence column. No repository file was created, modified or deleted — everything I wrote lives under the scratchpad path I was given.
- `prompts.ts` was present in the audited snapshot at 0% coverage (`reduce.ts` imports `answerPrompt`, but no M1/M2 test reaches an `answer` action). Its correctness is M3-T3's gate, not this one.
- Mermaid diagrams were read as source text, not rendered.

Verdict: **FAIL-MAJOR.**
BLOCKER ids: **none.**
MAJOR ids: **B-1, B-2, B-3.**
