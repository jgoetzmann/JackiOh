# JackiOh review — 2026-09-18 — SPEC.md revision 5 at commit `8f9ed57`, §11 at R1–R169
Scope: Part A (fifth run) — content audit of SPEC §11 rows **R82–R169**, plus A1.3 re-run and two standing carry-forwards
Verdict: **FAIL**

Produced by an independent reviewer session following REVIEW.md Part A. Read-only: no file in the repository was modified except this report.

**Result: 1 BLOCKER, 4 MAJOR, 11 MINOR.**

R82–R169 are the 88 rows no prior Part A has read for content. The four earlier reports (`2026-09-17-part-a.md`, `-r2`, `-r3`, `-r4`) audited §11 at R1–R81; their findings are not repeated here except for the two the assignment asked to re-state. Ids, numbering and cross-reference *resolution* were re-confirmed mechanically and pass (169 rows, R1→R169, no gap, no duplicate; the seven `decide` rows are exactly R1, R2, R4, R5, R14, R26, R39, which is what the §11 intro lists). What this pass adds is the reading nobody had done: each row against the §1–§10 sentence it claims to extend, against the §8 rows in its own "Cards affected" column, and against every other §11 row on the same subject.

**R82–R169 are not clean.** One row states a keyword rule backwards from both §6.1 and the source mechanics sheet; four more leave an engine contract or a resolution order that two implementers would build differently.

---

## Findings

| ID | Severity | Location (file:line or SPEC §) | Expected (SPEC § / source) | Observed | Evidence |
| --- | --- | --- | --- | --- | --- |
| A-1 | **BLOCKER** | SPEC.md:819 (R93) and :147, :155 (§4.3) vs :252 (§6.1); `JackiOh_Mechanics.md:82` | A1.1 / A2.4: First Strike means *a unit with First Strike* deals damage before units without — attacker or defender | R93 restricts it to the attacker, and §4.3's step 1 does the same, so a **First Strike defender attacked by a non-First-Strike attacker gets no benefit**. Concretely: #20 Pointmaster (7/2, First Strike) attacked by a 3/3 kills it and survives under §6.1 and the source; under R93 and §4.3 the exchange is simultaneous and Pointmaster dies. The engine already implements the §6.1 reading (`combat.ts:317` `if (defenderFirst && !attackerFirst) { strikeBack(); … }`), so the wrong artefact is the spec text, not the code | R93: "First Strike only moves the **attacker's** strike earlier"; §4.3 mermaid line :147 "A's attack, if A has First Strike and D does not" — the D-only case appears nowhere; §6.1 :252 "Deals damage before non-First-Strike units"; source: "Units with First Strike deal damage before Units without" / "If the opposing Unit dies from this Unit's damage, don't take any damage at all" |
| A-2 | MAJOR | SPEC.md:713 (§10.9) vs :828 (R102), :839 (R113), :842 (R116), :852 (R126), :879 (R153) | A3.1 / A3.5: every hook a ruling names is a member of the `Script` type | §10.9's `Script` enumeration has no `delayed`, no `resume` and no `setStat`. Five rulings treat all three as Script keys — R126 is *entirely about* which of `delayed` and `resume` a continuation lands on, R153 lists `setStat` among the hooks a field card answers, R102 merges "`resume` step tables" and sums "layer-2 `setStat`", R113 resolves a work item "against … the card's script table". BUILD.md:293 carries the same stale list, so the contract is wrong in both governing documents | §10.9 :713 `Script = { cost?, cry?, death?, startOfGame?, startOfTurn?, endOfTurn?, aura?, triggers?, activate?, onPlayHook?, handTriggers?, staticFlags?, targets?, modes? }`; R126 "`resume.hook` names a key of the card's `Script`"; the engine's own type has all three (`packages/engine/src/script.ts:117,119,121`) and 16 card scripts already export `resume` |
| A-3 | MAJOR | SPEC.md:879 (R153) vs :769 (R43), :877 (R151), :487 (#98), §4.5 step 3 | A3.4: the per-zone hook table is complete and agrees with the rulings it sits beside | R153 enumerates which hooks each zone allows and gets three things wrong. (a) It says a card **in a library answers nothing** and a card in a hand answers **only** `handTriggers` — but R43 requires "every Heroic Power in either player's hand **or library**" to roll `startOfGame` at setup, and R151 extends the roll to every arrival. (b) The field list omits `death`, although #3, #22, #81 and #86 all fire Death hooks from the field (§4.5 step 3). (c) It omits `activate`, although #98's once-per-turn ability is activated while it sits face-up in the backrow (R103) | R153 "in a hand it answers only `handTriggers` … in a library, in exile, in the resolving zone, or dormant under a Stack it answers nothing"; R43 :769 "At start of game every Heroic Power in either player's hand or library rolls its power"; §10.9 :713 names `startOfGame` as "Heroic Power's roll (R43)" |
| A-4 | MAJOR | SPEC.md:845 (R119) vs :689 (§10.5 step 5), :668 (§10.3), :407 (#38) | A2.9 / A3.4: the play pipeline and the self-arrival rule agree | R119 rules that a permanent does not answer the play that put it onto the field, and justifies it with §10.3's "an event only to the cards watching when it is dispatched". §10.5 puts the card on the field at **step 4** and emits `cardPlayed` there, so at dispatch the card *is* watching — R119's cited support argues the opposite of R119's conclusion. The divergence is live for **#38 Quickstriker**: §10.5 step 5 names Quickstriker in the pipeline of every play, including its own, while R119 says its own play is exempt. (#33 is settled either way because R17 fires it at step 7.) Two implementers reading §10.5 and reading R119 build different cards | §10.5 :689 "Resolve Combo checks, **Quickstriker**, /fullsend's Combo draw, then the card's own Cry"; :688 step 4 "Move the card to the field … emit `cardPlayed`"; R119 "it was not yet in play when that play began, and §10.3 offers an event only to the cards watching when it is dispatched" |
| A-5 | MAJOR | SPEC.md:882 (R156) vs :178–:182 (§4.5 steps 1–5) | A2.8 / A3.6: a state check interrupted by a prompt has a defined outcome for all five steps | R156 splits §4.5 across a prompt and accounts for steps 1, 2 and 3 only. Steps **4 (Reborn return)** and **5 (repeat until nothing changes)** are unstated. This is not academic: R83 gives the Reborn body the *current* turn as its `summonedTurn`, and R64 holds its zone reserved meanwhile, so whether the body is on the board while the prompt is open decides whether it can be targeted by the very effect that asked, whether the zone is free for anything else, and which turn it counts as sick for. An implementer may equally defer step 4 with step 3 or run it at once | R156 "§4.5 steps 1 and 2 still run … but step 3 is owed in full"; §4.5 :181 step 4 "Reborn: each collected unit that had Reborn returns to its reserved zone"; :182 step 5 "Repeat until nothing changes" |
| A-6 | MINOR | SPEC.md:359 (§8 rarity paragraph) vs :397 (#28), :411 (#42), :417 (#48), :429 (#54), :437 (#62), :446 (#70) | A1.3: the stated criteria predict the assigned rarity | **Standing from prior passes, still true at revision 5.** Counts are exactly right (recounted 35/37/16/7/5 = 100), but six Commons carry traits the paragraph reserves for higher tiers: #28 reads three zones (Epic: "effects that read several zones"), #42 rolls a Lucky chance (Rare), #48's radiant is a modal choice (Rare: "a modal choice or prompt"), #54 sets a cost override (Rare: "a cost or mana modifier"), #62 is two primitives (Common allows "one primitive with at most one target"), #70 reads two state values (Rare) | :359 "Common: keywords only, one primitive with at most one target, or one fixed start-of-turn, end-of-turn or cast-on-draw effect" |
| A-7 | MINOR | SPEC.md:723 (§11 intro), :3 (header) | A2.3: §11 states where each row came from | **Standing from prior passes, now much larger.** The intro still says the 2026-09-17 reviews "added R58 to R90" and lists the sections they edited. §11 now holds **R1–R169**, so **R91–R169 — 79 rows, 47% of the section — have no stated provenance**, and the edits they carry are not in the section list: §10.1 gained `work` and `workCursor` for R113, and §10.8 is amended by R169 without being named. The header still reads "revision 5 of 2026-09-17" although R169 was added 2026-09-18 | :723 "The spec reviews of 2026-09-17 added R58 to R90 … along with edits to §2.5, §3, §4, §4.4, §4.5, §5.1, §6.3, §8, §9.5, §10.1, §10.2, §10.6, §10.7 and §10.8"; :3 "2026-09-16, revision 5 of 2026-09-17" |
| A-8 | MINOR | SPEC.md:895 (R169) vs :404 (#35), :454 (#78), :455 (#79), :439 (#64), :621 (§10.1) | A1.3 / A3.5: a ruling's premise holds for every card it names | **R169 audited as requested.** Its conclusion — the modifier list travels on both seats — is **correct**, but it rests on a premise that is false for three of its four installers and its enumeration is incomplete. "Every modifier in the Core set is installed by the **Cry** of a card played face-up": #35 Lunar Eclipse and #78 /fullsend are **Spells**, which never enter the field and so can never have a Cry (§6.2, §10.5 step 4); #79 Twinspell is a Field Spell whose §8 row names no Cry. Only #77 Professor Curvature is a Cry (source: "**Cry**: Your (4) cost cards next turn cost (1) less"). The list also omits **#64 Gifted Program**, whose per-turn flag §10.1 names among `mods` and which is installed by a play hook, not a Cry. The load-bearing half — "played **face-up**" — does hold for all five: #35 and #78 are spells played publicly, #77 is a unit, #79 and #64 are Field Spells, and §3's zone table makes Field Spells visible to both. No Core trap installs a `mods` entry, so "both seats" is safe for Core; the premise as written would let a later set install one from a face-down trap and inherit a rule whose reason no longer applies | R169 "because every modifier in the Core set is installed by the Cry of a card played face-up"; §10.1 :621 `mods: PlayerModifier[]; // next-spell discount, this-turn discounts, Curvature, Twinspell echo, **Gifted flag**, /fullsend's Combo draw`; #78 :454 is typed "Spell"; #35 :404 "Spell". R169 also cites **CLAUDE.md rule 7** — a process file — where §9.1 and §10.8 are the in-spec homes for the same rule, so A2.1 cannot resolve the reference |
| A-9 | MINOR | SPEC.md:825 (R99), :826 (R100), :840 (R114), :846 (R120), :862 (R136) | A2.1: "Cards affected" names the cards the row governs | Five rows in this block cite cards that have nothing to do with them. **R99** ("a trap's condition") cites #52 Silly Silas, #56 Jilliax and #67 Zoomerbin Oomen — a unit, a vanilla-keyword unit and a unit; the traps are #18, #41, #60, #71, #85, #96. **R100** (the end-of-turn trap window) cites #52 and #62 Friend of Felinors; the two cards that fire in that window are #18 and #71. **R114** (Trample into a unit with no health) cites #21 Hinder, a cast-on-draw mana spell; no Core card prints Trample at all (§6.1: "Random-keyword pool only"), so the row's cards are #63 and #80. **R120** ("an 'Also' clause stands on its own") cites #53 Reno, which has no "Also" clause, and omits #34 Collateral Damage, which does — a grep of §8 finds "Also" in exactly two rows, #34 and #41. **R136** (per-script event window) cites #24 and #31, two end-of-turn return spells that ask nothing about what they just did. The indices all exist, so the mechanical A2.3 check passes; the content does not | `sed -n '361,490p' SPEC.md \| grep -n "Also"` returns only #34 and #41; §8 :427 #52 "Unit, Human", :431 #56 "Unit", :443 #67 "Unit, Human", :437 #62 "Spell", :390 #21 "Spell" |
| A-10 | MINOR | SPEC.md:851 (R125), :828 (R102), :832 (R106), :888 (R162), :892 (R166) | A2.1: a cited § says what the row says it says | Five section citations in this block point at the wrong place. **R125** says "the same treatment **§5.3** already gives CN-Virus's damage to its own controller" — §5.3 is the catalog-corrections table; that rule is in **§7**'s token rules (:349 "CN-Virus damage is a normal damage instance to your own hero"). **R102** attributes the `gone` tag to **R86**; `gone` is defined in R11 and §10.1 (:644), and R102 also extends it from §10.1's "a unit token that has ceased to exist" to any consumed Fuse ingredient without saying so. **R106** says "§9.8 asks for 'a threshold in a window'" — that sentence is in **§9.4** (:532). **R162** says "§9.1's client authenticates with a bearer token in a header"; §9.1 says no such thing. **R166** reads §9.5's queue population as a wait-time display; §9.5 (:538) says "shows the queue population" | quoted above; §7 :349; §10.1 :644 "`{ z: 'gone' }`, a unit token that has ceased to exist" |
| A-11 | MINOR | SPEC.md:835 (R109) vs :863 (R137) | A2.1: a superseded row says it is superseded | R109 still reads "**5 actions per second per match**". R137 replaces that scope with per-seat plus a match aggregate of twice it, and explains why the shared counter is itself an abuse vector. R137 names R109; R109 does not name R137, so a reader who stops at R109 implements the rule R137 exists to forbid | R109 "5 actions per second per match"; R137 "Per seat, not per match … Each socket therefore gets R109's allowance, the match's aggregate ceiling is twice it" |
| A-12 | MINOR | SPEC.md:830 (R104) vs :532 (§9.4); :838 (R112) vs :539 (§9.5) | A2.1: body text a ruling corrects points at the ruling | Two rulings in this block correct §9 and §9 was not annotated. **R104** shows §9.4's alphabet is impossible as written (36 alphanumerics minus 0/O/1/I/l = 31, not 32) and fixes it; §9.4 still reads "a 32-symbol alphabet without 0/O/1/I/l" with no pointer. (R104's own alphabet is correct: `23456789ABCDEFGHJKLMNPQRSTUVWXYZ` counts 8 + 24 = 32, so 16 characters carry exactly 80 bits.) **R112** makes a reaper-resolved ceiling draw leave both ratings unchanged, against §9.5's unqualified "Ratings use an Elo update (K = 32)" — R112 calls the consequence deliberate, but §9.5 does not carry the exception | §9.4 :532; R104 :830; §9.5 :539; R112 :838 "records `turns = 0` and leaves both ratings unchanged" |
| A-13 | MINOR | SPEC.md:859 (R133) vs :408 (#39) | A1.3: §8's cell and its governing ruling agree on a count | R133 makes a play pool "the set of cards played, not the list of plays" and names #39 in its Cards affected. §8's #39 cell says a copy is made "of **every card in `turnLog.playedIds`**", and R133 itself says playedIds "records one entry per play, so a card played, bounced and replayed appears twice". Read through R133 a twice-played card yields one copy; read off §8 it yields two. Reachable with #17 Flood or radiant #52 plus the mana #78 supplies | R133 :859; #39 :408 "a fresh copy (radiant flag kept) of every card in `turnLog.playedIds` except this one" |
| A-14 | MINOR | SPEC.md:823 (R97) vs :759 (R33), :709 (§10.8), :382 (#18), :447 (#71) | A2.6: the redaction rule and the visibility rules agree | R97 keys `trapFired`'s `instanceId`/`defId` to the trap's controller and justifies it with "firing the trap **consumes it into a public graveyard**". That is false for Field Traps: #18 Bread and Butter and #71 Intern Stimmy stay in the backrow after firing and become **face-up to both** (R33, §10.8). So the opponent reads the sentinel in an event about a card they can see in full in the same view, and Intern Stimmy re-fires every qualifying turn end. No information leaks — the rule only over-redacts — but the stated reason does not hold for a third of the trap pool | R97 "because firing the trap consumes it into a public graveyard"; R33 :759 "A Field Trap that has fired is face-up to both"; §10.8 :709 "a Field Trap that has fired is public" |
| A-15 | MINOR | SPEC.md:855 (R129), :856 (R130) vs :700 (§10.7), :523 (§9.3), :757 (R31) | A3.3: a randomness rule's justification is sound | Both rows defend themselves with a replay argument the spec does not make. §9.3 says "(seed, log) reconstructs any match" — a fold replays *the same* log, so two boards never diverge mid-replay and neither rule is needed to keep a cursor in step. R129 goes further and asserts the opposite of what it rules: making a fizzle draw nothing is precisely what makes `rngCursor` depend on the board. R129's example is also wrong — #76 Field of Dreams *replaces* the hand, and R31 sends the replaced cards to the graveyard; there is no discard, random or otherwise, anywhere in it | R129 "so `rngCursor` never depends on the board at that moment … it is why a whole-hand discard is its own verb"; #76 :452 "Replace your hand with the same number of Reminisce"; R31 :757 |
| A-16 | MINOR | SPEC.md:817 (R91), :857 (R131), :842 (R116) vs :772 (R46), :317 (§6.3), :113 (§3.2), :858 (R132), :876 (R150) | A2.1 / A3.1: a row's statement fits the cards it names | Three rows in this block are stated against the wrong case. **R91** ("switching to the position a unit already holds") cites #48 and #65.1, but §6.3 makes `switchPosition` a *flip*, which can never name the position already held; the cases R91 actually governs are R46's forced switch to Attack and §6.3's refusal to put Spikey Pillow in Defense, neither of which it mentions. **R131** restates the Felinor set as "every other Felinor **you control**", dropping §8 #92's and §3.2's explicit exception that dormant cards under a Stack count, and its "a second Felinor Fiender contributes…" clause presumes a Felinor tag #92 does not print (§8 and the source both tag it "Unit, Human"). **R116** says layer 2 adds the hook's result "each component floored at 0", where R132 and R150 put the floor on the combined total and R150 forbids flooring contributors | R91 :817; §6.3 :317 "Flip Attack and Defense"; R46 :772; R131 :857; §3.2 :113 "Felinor Fiender's 'including those under a Stack' is the one exception"; #92 :474 "Unit, Human"; R116 :842 vs R150 :876 "the summing read must not invent one" |

## Suggested fixes (not applied — SPEC.md was not touched)

- **A-1**: add to §4.3 a step-1 line for the defender (`D->>A: D's attack, if D has First Strike and A does not` / "If A is destroyed here it deals nothing"), and reword R93 to "First Strike only moves **that unit's** strike earlier" with a sentence naming the defender case. The engine at `combat.ts:309–330` is already the intended behaviour and needs no change.
- **A-2**: extend §10.9's `Script` to `{ …, delayed?, resume?, setStat? }` and mirror it in BUILD.md:293.
- **A-3**: add `death` and `activate` to R153's field list, and carve out `startOfGame` for hand and library with a pointer to R43.
- **A-4**: decide #38 explicitly. Either add "except the play that put it there" to §10.5 step 5, or narrow R119 to triggers registered on `cardPlayed` and exempt the step-5 pipeline.
- **A-5**: give R156 a sentence for steps 4 and 5 — the natural reading, consistent with R64's reservation, is that step 4 is owed with step 3 and the zone stays reserved across the prompt.
- **A-8**: replace R169's premise with "every modifier in the Core set is installed by a card played **face-up** (#35, #64, #77, #78, #79)"; the conclusion is unchanged. Add #64 to the Cards affected, and re-cite CLAUDE.md rule 7 as §9.1/§10.8.
- **A-7**: rewrite the §11 intro's provenance sentence to cover R91–R169 and add §10.1/§10.8 to the edited-section list; date the header 2026-09-18.

---

## Coverage of checks

| Check | Result | Evidence |
| --- | --- | --- |
| **§11 R82–R169 content read** (the assignment) | **fail — 1 BLOCKER, 4 MAJOR, 11 MINOR** | Method below. Every one of the 88 rows was compared against three things: the §1–§10 sentences it cites, the §8 rows in its own Cards-affected column, and the other §11 rows on its subject. See "How R82–R169 were checked" |
| A1.1 Keywords | pass, except A-1 | All 57 mechanics terms located in §3–§7. The one substantive mismatch with the source text is First Strike (A-1); Flicker's deliberate absence is still stated at :326 and no §8 cell uses it |
| A1.2 Systems | pass | Every source number re-checked against §2–§4 and unchanged: 1→4 mana, draw 1, 30-turn cap, auto-skip (R82), P1 3 / P2 4, mulligan, 20 cards no duplicates, 30 health, 5 unit + 5 backrow zones, the self-copy exclusion with §5.1's "unless the card names the pool" escape |
| **A1.3 Cards (card-by-card diff, first run against revision 5)** | **pass** | `JackiOh_Core_Cards.md` **is present** (25,514 bytes) and the diff was run. **105 source headers, 105 §8 rows, every index exactly once, no duplicates, nothing source-only or spec-only.** 0 cost diffs after normalising the source's `(2 embiggened 4\)` to §8's `2 embiggen 4`; **0 base-stat and 0 radiant-stat diffs** across all 105; 0 radiant-separator mismatches (no card with a `~~~` in the source lacks a radiant cell, none with "No radiant form" has one). The only name diffs are #86 (`"Miss" Mrow` quote style) and #90.1 (`CN-Virus`, the §5.3/R75 rename). Every type/tag diff is either a §5.3 row (#2 set, #12/#31/#51 header typos, #68 Unit, #15/#34/#66/#86/#99/#100 missing type) or a parser artefact of a source header whose name contains a classifier word (#8 "Human Common" is a missing comma in the source). Rarity recount **35 Common / 37 Rare / 16 Epic / 7 Legendary / 5 Mythic = 100**; the source grouping differs on 60 rows, which :359 declares intended. Clause fit: A-6 and A-13 only. Script: `scratchpad/diff2.py` |
| A1.4 Tokens | pass | §7 has exactly 9 rows; Lava Golem is card #55 not a token; the 5 card-defined tokens 51.1, 65.1, 90.1, 93.1, 95.1 are in §8 and §7 agrees on cost, stats and text; "Generated by" lists reproduce by grep |
| A1.5 Architecture | pass, with A-10/A-11/A-12 | Every `ARCHITECTURE-CCG.md` section has a §9 counterpart. R104–R112 and R137–R167 fill the numbers §9 left open (alphabet, version format, breaker, padding, cadences, limits, code reuse, grant, reaper, CORS, catalog route, ban list, queue exit, pairing). Three of them correct §9 without §9 pointing back (A-12), one is superseded without saying so (A-11) |
| A2.1 Cross-references | pass mechanically, fail on content (A-9, A-10) | All `§n` and `R<n>` references resolve — 0 dangling. But five rows cite cards they do not govern and five cite the wrong section |
| A2.2 Counts | pass | §1 says 100 + 9; §7 has 9 rows; §8 has 100 non-token + 5 token rows; the rarity sentence sums to 100 and matches a recount of the tables |
| A2.3 Rulings | pass, with A-7 | 169 rows, R1→R169, no gap, no duplicate (`grep -c "^| R[0-9]\+ "` = 169). Every Cards-affected index exists in §8. The `decide` rows are exactly R1, R2, R4, R5, R14, R26, R39 — the seven the intro names. Provenance is the gap (A-7) |
| A2.4 Pipeline order | **fail (A-1)** | §4.4's ten steps are used identically everywhere, and R114, R124 and R125 all sit correctly inside them (R114 folds into step 5's Trample carve-out; R124 stacks hero armor at step 2 against the step-3 cap; R125 puts fatigue through the whole pipeline). §4.3's combat order is the break |
| A2.5 Pools | pass | Recomputed from §8: KY-tagged = {31, 51, 82}; Trap-type = {18, 41, 60, 71, 85, 96}, all cost 1 (so #67's two forms share a pool, as its cell says); §8-Legendary minus #83 = {52, 85, 87, 92, 93, 95}, matching R35 and #83's cell exactly |
| A2.6 Token rules | pass, with A-14 | §3.2, §6.3, §7, R11, R34, R80 and R88 agree on tokens; R88 correctly routes a whole-board move into a closed zone through R78, R4 and R11. The residual is R97's trap redaction against R33's fired Field Trap |
| A2.7 Radiant rules | pass | No §8 Engine cell contradicts §5.2. R151's arrival roll is consistent with R43 and R78 |
| A2.8 Cry rule | pass, with A-5 | R1 is stated once and every copy/recruit/reborn row agrees. R118 correctly reconciles R17's "Cry is lost" with R1's exactly-once. The state-check gap is A-5 |
| A2.9 Turn structure | pass, with A-4 | The sequence is identical in §2.2, §6.2, §10.3 and R62; R100 closes the double-delivery hole in the trap window; R82's auto-end matches §2.5 :80. The play pipeline is where §10.5 and R119 part |
| A3.1 Primitives | pass, with A-2, A-16 | Every §8 verb has a §6.3 row or a §10.4/§10.7 helper. R102 composes Fuse member by member and is consistent with R77 on every field it repeats. The missing Script members are A-2 |
| A3.2 Prompts | pass | Every card choice maps to a §10.6 kind or a play-action field per R81/R90/R123. R103's "prompting otherwise" for a Heroic Power ping opens a `target`, a kind §10.6 allows |
| A3.3 Randomness | pass, with A-15 | Every random effect names its pool and exclusions or defers to `catalog.query`; R129 and R130 are correct as rules and unsound as arguments |
| A3.4 Timing | pass, with A-3, A-4 | Every trigger names its moment; R121 correctly separates a forced attack from a declaration; R96 closes the already-gone attacker; R100 fixes the double trap-window delivery; R152 pins the AI lockout to the turn §8 names |
| A3.5 State | pass, with A-2 | Every remembering card names its §10.1 home. R113's `work`/`workCursor`, R117's ownership rule and R122's "the answering action finishes the sequence" are mutually consistent and all three are reflected in §10.1 :603–:604 |
| A3.6 Unbounded loops | pass | Terminators re-checked and all present: R28/R87's cap with its at-the-cap rule, R58's per-draw cap with R80's library cap, R1, #3's base-copy chain, grade S, R41, #85 never matching itself, R156/R113's raise-on-unknown-hook |

### How R82–R169 were checked

"I read them all and found nothing" is not a result, so here is the comparison set, block by block. Each row was read against the § it cites, the §8 rows in its Cards-affected column, and the neighbouring rulings on the same topic.

| Rows | Compared against | Outcome |
| --- | --- | --- |
| R82–R84 | §2.5 auto-end, R36, §10.7's AI policy, R44 | consistent; R82's "a draw offer does not hold the turn open" is a deliberate narrowing of R36 and says so |
| R85–R89 | §4.4 steps 5–9, §4.5 steps 1–4, R11, R42, R63, R78, §6.1 Lifesteal | consistent; R89's "the event carries what the card was" is exactly what §4.5 step 3 now says |
| R90, R101, R123 | §10.5 step 1, §9.3, §6.3 Tribute, §3.2's Sheep = 2, R81, R13 | consistent — the target/Tribute split (fizzle vs illegal) is stated twice and agrees both times |
| R91–R96 | §4.1, §4.2, §4.3, §4.4 steps 9–10, §6.3 Switch/Forced attack, R13, R46, R53, R59, R63 | **A-1** (R93), A-16 (R91); R92, R94, R95, R96 consistent |
| R97, R154, R168 | §9.1, §10.8, §10.10, §10.3's event list, R33 | A-14; R154 and R168 consistent, and R168's floor is what makes M5-T4's `attackCancelled` row reachable |
| R98, R113, R117, R122, R126, R127, R156, R158 | §9.3, §10.5 steps 4–8, §10.6, §10.1's `work`/`workCursor`, §2.1 step 3, §2.4, R9 | **A-5** (R156); the other seven are mutually consistent and form a closed account of pausing and resuming |
| R99, R100, R118, R119, R121 | §10.3's resolution loop, §10.5 steps 4–7, R1, R17, R61, R62 | **A-4** (R119), A-9 (R99, R100); R118 and R121 consistent |
| R102, R103, R139, R151 | §6.3 Fuse, R23, R43, R55, R77, §10.9, §8 #85/#98/#99 | **A-2**, A-3, A-10 (the R86 citation); the Fuse composition itself agrees with R77 field by field |
| R104–R112, R137, R141–R149, R157, R159–R167 | §9.1, §9.2, §9.4, §9.5, §9.8, R79 | A-10, A-11, A-12; otherwise each fills a number §9 left open without contradicting it |
| R114–R116, R124, R125, R128, R131, R132, R150 | §4.4, §10.4's six layers, §3.2, §6.3 Vanilla, R39, R51, R59, R63, R69 | A-10, A-16; the layer account (delta not total, no per-contributor floor, floor on the total) is otherwise coherent |
| R120, R129, R130, R133–R136, R138, R140, R152, R153, R155 | §8's reading conventions :357, §2.4, §3.2, §5.1, §6.3, §10.5 steps 5–7, R3, R4, R16, R31, R32, R64, R70 | **A-3** (R153), A-9, A-13, A-15; R135, R138, R152 and R155 consistent |
| R169 | §10.1 `mods`, §10.3, §10.8, §8 #35/#64/#77/#78/#79, R48, R97, source headers for all four named cards | **A-8** — conclusion sound, premise false for 3 of 4, list short by one |

---

## Not verified

- The audit is against `SPEC.md` as committed at **`8f9ed57`** ("Record what happened to each review finding, without editing the reviews"); `git status` shows SPEC.md clean, so nothing here reads an uncommitted edit. Other agents were live in the tree during this pass (four e2e files showed as modified); none of them touch SPEC.md or the source design files.
- **Part B was not run** by assignment. The full test suite, the fuzz gate, coverage and Cypress were deliberately not executed (contended). Two code facts are quoted as *evidence about the spec*, not as a Part B result: `packages/engine/src/combat.ts:309–330` (A-1, the engine already does the right thing) and `packages/engine/src/script.ts:117–121` plus a grep showing 16 card scripts export `resume` (A-2, the engine type already has the missing members). Neither was confirmed by running a test.
- **R82–R81 overlap**: rows R1–R81 were re-read only where an R82–R169 row cites them. The four prior reports' findings on those rows were not re-litigated.
- **Rarity** was checked for counts and set membership; fit to the §8 criteria is a judgement call (A-6), unchanged in method from the prior runs.
- **Mermaid diagrams** (§2.2, §4.3, §9.2, §10.3) were read as source text, not rendered. A-1 turns on the §4.3 diagram's text.
- The Hearthstone conventions invoked by R3, R59, R69 and R83 were taken at face value.
- A-4, A-5 and A-13 are reasoned from the rules text; no fixture was traced through an engine to confirm the divergence is live in the current build.
- R168's "38 events in one reduction" and R169's "no component rendered that element" are claims the rows make about a past build; they were not re-measured.

---

**Verdict: FAIL.**

**BLOCKER ids: A-1.**

**MAJOR ids: A-2, A-3, A-4, A-5.**

MINOR ids: A-6, A-7, A-8, A-9, A-10, A-11, A-12, A-13, A-14, A-15, A-16.
