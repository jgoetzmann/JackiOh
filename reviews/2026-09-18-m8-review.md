# JackiOh review — 2026-09-18 — 7c476c9316fdaba3a7b674cfc46bb2200386ccff

Scope: Part A (re-verification only) | Part B (milestone M8), concentrated on B7 and B8
Verdict: **FAIL-MAJOR**
BLOCKER ids: **none**

Working tree at review time is not clean: `e2e/cypress/e2e/06-room-code.cy.ts` and
`e2e/cypress/e2e/09-deckbuilder.cy.ts` carry uncommitted edits (`git status --porcelain`).
Everything graded below is the **committed** state at 7c476c9, which is the state the reported
green CI run measured. The uncommitted 09 rewrite is recorded but not graded (it is in flight).

---

## Findings

| ID | Severity | Location (file:line or SPEC §) | Expected (SPEC § / BUILD) | Observed | Evidence |
| --- | --- | --- | --- | --- | --- |
| A-1 | MINOR | SPEC §8 rarity paragraph, SPEC.md:359 | The stated rarity criteria predict the §8 rarity column | Six Commons read like Rares or Epics under the stated criteria | #28 SPEC.md:397 Common yet reads library+hand+field; #48 SPEC.md:417 Common yet radiant "Choose: all enemy units, or all units"; #54 SPEC.md:429 Common yet `costOverride`; also #42:411, #62:437, #70:446. Counts are correct (35/37/16/7/5 recomputed). Standing since r1 A-31 → r2 A-25 → r3 A-14 → r4 A-8. |
| A-2 | MINOR | SPEC §11 intro, SPEC.md:723 | The intro accounts for the rows §11 actually holds | Intro says the reviews "added R58 to R90 … and R83–R90"; §11 runs to **R168**. R91–R168 have no provenance sentence | §11 first row SPEC.md:727, last row SPEC.md:894; 168 rows, R1–R168, zero gaps, zero duplicates. r4 A-6 in a larger form: SPEC.md:3 now does carry "revision 5 of 2026-09-17", so half of A-6 is fixed; the intro is now affirmatively wrong about the range. |
| B-1 | MAJOR | `e2e/cypress/e2e/09-deckbuilder.cy.ts` (at HEAD) | BUILD M8 row 09: "each of L1–L6 **shows** its message; a card **dragged** into a second deck is refused; save succeeds when legal" | All nine `it`s assert against `PUT /api/loadout` JSON. No drag is performed anywhere in the suite; no deckbuilder testid is referenced by any spec | `git grep -n dragCardToDeck HEAD -- e2e/cypress/e2e` → **no hits** (the command exists only at `e2e/support/commands.ts:757`, unused). `git grep -n 'DECKBUILDER\|LOADOUT_ERRORS\|loadoutErrorId\|cardPoolId\|deckCardId' HEAD -- e2e/cypress/e2e` → **no hits**. The rule assertions run through `expectRefusal(...)` on `response.body.error.details` (HEAD:130–147). Two of the row's three clauses are measured at a layer the row does not name. |
| B-2 | MAJOR | `e2e/cypress/e2e/10-invite-gate.cy.ts:229-233` | BUILD M8 row 10: "**code screen shown**" | Proven only as a URL: `cy.location("pathname").should("eq", routes.invite())`. Nothing asserts the screen renders anything — no input, no submit, no error element. The three failure kinds are exercised only over HTTP (`cy.request`), never typed into the screen | The spec says so itself at 10-invite-gate.cy.ts:226-228: "ASK … `invite-code-input`, `invite-submit` and `invite-error`, without which the screen's own contents and the typed-in-a-bad-code path cannot be asserted from a spec". An `/invite` route that rendered a blank page would pass this row. |
| B-3 | MINOR | `e2e/cypress/e2e/05-reconnect.cy.ts:463-472` | An assertion that can fail | `for (const frame of frames) { const grace = frame.clocks.graceDeadline.p1; if (grace === null) continue; expect(...) }` — if seat 2 never observed a non-null p1 grace, the loop body never runs and the block asserts nothing | This is the suite's only use of `DISCONNECT_GRACE_SECONDS` as a bound. It is conditional by design ("Any grace countdown seat 2 did observe") and BUILD's row does not ask for grace, so MINOR — but it is the one place in the twelve specs that can pass while measuring nothing. |
| B-4 | MINOR | `05-reconnect.cy.ts:309-314`, `06-room-code.cy.ts:230-235` | BUILD M8 preamble: "Every spec sets a seed" | Both send `{deckIndex: 0, seed}` and neither asserts the seed reached the engine | Acceptance **is** proven end to end: under R143 a non-E2E server answers 400 (`apps/server/src/match/rooms.ts:163`, `:206`) and `cy.request`'s default `failOnStatusCode: true` would fail the spec — so a green 05/06 proves the server took the seed. **Usage** is proven only by `apps/server/test/match/rooms.test.ts:162` ("R143 uses the host's seed verbatim for the match the join creates, in end-to-end mode"). The hotseat specs do better: `cy.seedGame` asserts `handle.seed === seed` at `e2e/support/commands.ts:350-352`. |
| B-5 | MINOR | `e2e/cypress/e2e/06-room-code.cy.ts:219-221` at HEAD | A comment that is true | HEAD reads "a networked seed is minted by the server, so honouring a supplied seed under E2E=1 is an ASK (see the report). The fixture decks are written not to need one." R143 makes that false | `git diff e2e/cypress/e2e/06-room-code.cy.ts` shows the correction exists **only in the uncommitted working tree**; the diff is comment-only (no code change). Spec 05's equivalent comment was already corrected at HEAD (05-reconnect.cy.ts:296-308). |
| B-6 | MINOR | `09-deckbuilder.cy.ts:234`, `10-invite-gate.cy.ts:162,165` | BUILD M8 preamble: "Every spec sets a seed" | Both seeds reach nothing. Neither spec starts a game; the seed is asserted only with `expect(seed).to.be.a("string").and.not.eq("")` | Satisfied literally, not in substance. Both files document it. All ten other specs' seeds reach the engine (hotseat: `handle.seed`; networked: R143). |
| B-7 | MINOR | `.github/workflows/ci.yml` | BUILD §4: "plus a nightly run of `01` over 20 seeds" | No scheduled workflow exists. `ci.yml` is the only workflow and runs `on: push` / `on: pull_request` with no `schedule:` | The mechanism exists (`seedFor` + `--expose seed=`, `e2e/support/config.ts:22-24`); the job does not. BUILD §4 test strategy, not an M8 acceptance row. |
| B-8 | MINOR | `.github/workflows/ci.yml` e2e job | `e2e/package.json:10` declares `check:fixtures` | The e2e job runs only `cypress run --browser …`; `check-fixtures.mjs` never executes in CI | Nothing is unchecked in practice: `asDeck` in `e2e/support/commands.ts:153-167` re-validates size, duplicates and catalog membership at runtime on every `seedGame` / `installLoadout`. I ran the standalone gate by hand: **"28 deck fixture(s) OK"**, exit 0. |
| B-9 | MINOR | REVIEW.md B8, second bullet | "Pick 3 recorded logs from **the fuzz artefacts**" | There are no fuzz artefacts. `packages/cards/test/fuzz.test.ts` persists nothing; it prints its counts to stdout (`:517-527`) and writes no file | The only recorded-log artefact in the repo is `e2e/artifacts/01-hotseat-full-game.json` (gitignored) and its committed twin `packages/cards/test/fixtures/01-hotseat-full-game.json`. A reviewer following REVIEW literally finds nothing to pick from. This is a defect in REVIEW.md, not in the code. I satisfied B8 by re-deriving three fuzz logs from their seeds in one process and folding them in a second — see B8 below. |

---

## B7 — the twelve rows, checked clause by clause

`grep -rn "cy.wait([0-9]" e2e/` → **no output, exit 1**. A broader sweep found **zero** `cy.wait(…)`
calls of any form under `e2e/` (only comments naming the rule), zero `setTimeout` in specs or
browser-side support, and zero `.only` / `.skip` / `xit`. The rule is also **enforced at run time**,
not merely documented: `e2e/support/e2e.ts:7-22` overwrites `cy.wait` to throw on a numeric first
argument. All twelve files exist under `e2e/cypress/e2e/`; 33 `it` blocks at HEAD, matching the
reported "33/33 tests" on both browsers.

| Spec | BUILD "Key assertions" | Verdict | Where each clause is actually measured |
| --- | --- | --- | --- |
| 01 | result overlay appears; final state hash equals the vitest replay of the recorded actions | **pass** | Overlay `:197` + viewer-relative Win/Loss/Draw and the reason `:202-204`. Hash: `cy.replayCheck` (`commands.ts:808-826`) → `cy.task("replayHash")` → `e2e/support/tasks/replay-runner.ts`, which folds `(seed, decks, log)` through `packages/engine/src/replay.ts` in a **child process** and compares `hashState(fold)` against `hashState(browser state)`; it also asserts `errors` is `[]`. The "vitest replay" is not a paraphrase — `packages/cards/test/hotseat-replay.test.ts` is a literal vitest fold of the same committed log with `EXPECTED_HASH = "b2a3f515"` written down (`:63`), plus three negative controls (`:101`, `:115`, `:121`). I reproduced that hash independently (see B8). |
| 02 | each of the ten pickers rendered once and answered, whether it builds `play` or answers a `PendingChoice` (R81) | **pass** | Ten `it`s, one per kind (`mulligan, zone, discover, target, hand, mode, tribute, direction, x, embiggen`). `pickerOpensOnce` (`:155-159`) asserts exactly one prompt of that kind **and** exactly one prompt in the DOM; `pickerAnswered` (`:162-164`) asserts it is gone; each test then asserts the **effect** (lane 3 not lane 1 `:235-237`; discovered defId in hand `:270`; graveyard count "2" `:303`; `data-radiant="true"` `:325`; bounced unit back in hand `:358-359`; graveyard "3" and a 10/5 Armor-3 golem `:399-407`; ring step `:439-440`; hero at 28 `:467`; a 7/7 reading 2/2/2 `:493-495`). Each test also asserts `state.pending` is `null` or non-null per R81's split. Fixture `02-prompts-a.json` contains all ten cards BUILD names (verified against `packages/cards/catalog.json`). |
| 03 | trap flips during P1's turn; the unit is a Sheep; P1 can continue | **pass** | `cy.expectAnimating("trapFired")` `:109` + `state.active === "p1"` `:112`; Sheep Token in the chosen zone at 1/1 `:120-124`; R17's suppressed Cry proven by counting occupied zones `:131-137`; "can continue" = `end-turn` not disabled `:147`, a second play resolving its own Cry `:148-153`, and the turn finishing normally `:158-163`. |
| 04 | illegal target not highlighted; DEF card rotated; damage pops match pipeline numbers | **pass** | `expectHighlight(..., false, ...)` asserts `data-legal === "false"` (`:93-97`) — a value, not an absence. DEF: `data-position="DEF"` **and** `style` containing `rotate(90deg)` `:157-158`, plus the granted Taunt and Armor 1 `:160-161`. Pops: 2 / 3 / 3 / 3 / 7, every number computed from §4.3 and §4.4 in the header **before** the run and each followed by the durable stat it implies (`:172-181`, `:210-221`, `:226-236`). |
| 05 | same view and same open prompt after reload; clock kept running | **pass** | `fingerprint()` `:235-265` takes the whole sorted `[data-testid]` set + prompt kind + counters; deep-equal across `cy.reload()` `:399-410`, including the `PendingChoice` option keys. Non-vacuous: `:364-370` first asserts the prompt is `mode` with exactly 3 options. Clock: same absolute `turnDeadline`, later `now`, strictly less remaining, still > 0, same `ceilingAt`, p1 grace cleared (`:432-458`), and more clock frames after than before (`:422-428`). The rebuilt prompt is then **answered** `:478-484`, which is the proof it is live state. One weak spot → B-3. |
| 06 | both see the board; actions round-trip; game ends and both are queue-eligible | **pass** | Board: browser heroes + 20 zones + both hand counts `:291-300`; seat 2's view asserts 5 unit lanes, 5 backrow lanes, both heroes at 30 `:302-319`. Round-trip both directions: browser `endTurn` → seat 2 sees `active: p2`, `turn ≥ 2` `:361-368`; seat 2 `endTurn` → browser `end-turn` re-enables and seat 2 sees `active: p1`, `turn ≥ 3` `:375-385`. (`waitForMyTurn` is meaningful: `apps/web/src/routes/match.test.tsx:99` proves `end-turn` really is disabled off-turn.) Ending: draw offered and accepted, `expectResult("Draw")` + seat 2's view `winner: "draw"`, `phase: "over"` `:390-408`. Queue-eligible: `POST /api/queue` → 200 for both `:416-437`. Also asserts hidden information: opponent hand is a count, and the browser renders exactly that many cards `:338-352`. |
| 07 | attack cancelled; P1's controls disabled; AI actions animate; turn ends | **pass** | Cancel: `cy.expectAnimating("attackCancelled")` `:301` **and** hero health unchanged `:330`, plus the trap in the graveyard `:333-340`. Controls: `end-turn` disabled, `offer-draw` disabled, a held hand card carrying `data-legal="false"` `:318-326`. AI animates: `animating("turnEnded")` `:311`. Turn ends: `turn` advanced and `active === "p2"` `:342-343`. R99 is checked on **every** non-lethal swing (trap still in its zone, not in the graveyard, still face-down to the declarer) `:255-266`. Note: the bare `cy.get(ANIMATING).should("exist")` at `:310` adds nothing the line above it does not already prove — the specific `turnEnded` assertion is what carries the clause. |
| 08 | after the 30th player-turn the overlay says Draw | **pass** | R2's arithmetic in both halves: `turnsStarted === 15` for each seat `:104-105`, `turn ≥ 30` `:106`; `result.winner === "draw"` `:110`; both heroes still at 30, which is what makes it the **cap's** draw and not §2.5's other one `:111-112`; overlay text asserted from both seats `:121-123`. R82 is proven by counting presses: `pressed < 30` `:116`. |
| 09 | each of L1–L6 **shows** its message; a card **dragged** into a second deck is refused; save succeeds when legal | **FAIL** | See **B-1**. Only "save succeeds when legal" is measured at the layer the row names, and even that is a `PUT` rather than a click. In flight. |
| 10 | code screen shown; bad code error identical for three failure kinds; good code activates | **partial** | Clauses 2 and 3 are strong: the three failure bodies are compared as **bytes** (`new Set(bodies).size === 1` `:274-279`), one status, and the spread bounded, with each kind also asserted individually for status, code, message, absent `details` and R107's floor `:252-271`; activation is asserted through `/api/auth/me`, the 100-card collection ledger at quantity 1, a 409 on re-redemption, an unchanged ledger, a legal loadout save, and the gate opening `:289-388`. Clause 1 → **B-2**. |
| 11 | glow animation; stats swap on the field card keeping damage | **pass** | Glow: `cy.expectAnimating("radiantSet")` caught live by writing the one-click play out `:288-292`, plus `.radiant` on the hand card `:246`. The clause this spec exists for: `6/7` shown on a 7/7 body `:298`, and on the state — `radiant === true`, `damage === 1`, `buffs` unchanged, **same `defId` on the same instance id** `:299-309`. The precondition that makes the R60 pick deterministic is asserted before the cast `:259-281`, so a broken #94 fails readably instead of turning the test into a coin flip. |
| 12 | every card testid moves one lane; board swap flips sides | **pass** | The ring is written out (`RING`, `:105-116`) and every destination is one step along it, so a naive per-side shift fails. All six tracked cards — which is every card on the board — are asserted twice, in the DOM (`expectCardAt`, nesting) and in the engine (`expectEngineAt`) `:384-398`. Swap: the same six, lane-preserving, across the line `:412-425`. R33 is read twice over: seat 1's own trap becomes unreadable although it still owns it, seat 2's becomes readable `:424-427`. |

### Supporting machinery, checked for the "passes while measuring nothing" patterns

- **`should` callbacks with no `expect`**: swept all twelve specs and `support/commands.ts` — **zero**.
- **`cy.get` standing where an assertion belongs**: none found that matters. Cypress fails a bare
  `cy.get` on 0 elements, so every bare get is at least an existence assertion; every BUILD clause
  that asks about a *value* is asserted as a value (`have.text`, `have.attr`, `data-legal="false"`,
  deep-equal fingerprints, computed pipeline numbers).
- **Assertions inside a callback that never runs**: one real instance, B-3. Every other
  `if (… === undefined) return` is preceded by an `expect(…).to.not.eq(undefined)` on the same line
  above (`02:259`, `07:218`, `11:234`), so the guard cannot silently skip.
- **Existence asserted where a value was asked for**: spec 10 clause 1 (B-2). Nowhere else.
- **`cy.expectAnimating` cannot pass trivially**: `cy.settled()` (`commands.ts:213-215`) asserts
  `[data-animating]` does **not** exist after every action, so a permanently-present attribute would
  fail elsewhere in the same run. The two commands are mutual guarantees.
- **`cy.task("replayHash")` is not a self-comparison**: `replayHash` comes from folding the log in a
  child process; `browserHash` is `hashState` of the browser's own state
  (`replay-runner.ts:41-55`). Confirmed independently below.
- **`cy.wsPlayer` can fail**: `awaitView` waits on a predicate over real pushed frames with a 15 s
  reject (`wsPlayer.ts:101-109`, `:258-270`), and `matchesView`'s `promptKind` requires
  `pending.forYou === true` (`:120-124`), so it cannot be satisfied by the other seat's prompt.
  `cy.wsPlayer` asserts `result.ok === true` with the server's own words (`commands.ts:828-834`).
- Note: `hashState` excludes `state.applied`, the nonce log (`packages/engine/src/replay.ts:21`), so
  an equal hash does not by itself prove the dedupe map matched. B1.4's nonce test covers that.
- REVIEW B1.5's grep `grep -rn "canAttack\|isLegal" apps/web/src` returns 9 hits, all of
  `isLegal(highlight, testId)` — `apps/web/src/game/Card.tsx:25-28` is
  `highlight.legal.has(testId)`, a Set lookup over the `ActionBody[]` `legalActions` returned
  (`actions.ts:436 highlightFor`). Not client-side rule logic. REVIEW's grep produces a false
  positive; B1.5 holds, and it is what makes spec 04's `data-legal` assertions meaningful.

---

## B8 — determinism, folded in a fresh process

REVIEW's "fuzz artefacts" do not exist (B-9), so I reconstructed three and added a fourth:

**Process 1 (record)** played fuzz seeds 1, 500 and 1000 with the shipped policy exactly as
`packages/cards/test/fuzz.test.ts:207-256` does — `subsystems.chooseAction` over `legalActions`, the
same `jackioh-fuzz-decks-<n>` / `jackioh-fuzz-<n>` / `jackioh-fuzz-policy-<n>` seed strings — and
wrote `{seed, decks, log, liveHash}`. **Process 2 (fold)** read that file with no other input and
folded each log through `packages/engine/src/replay.ts` `fold`.

| Recorded log | actions | rejected by fold | fold hash | recorded hash | result |
| --- | --- | --- | --- | --- | --- |
| `jackioh-fuzz-1` | 103 | 0 | `0d0272ca` | `0d0272ca` | **MATCH** — p2 wins, hero-death, turn 28 |
| `jackioh-fuzz-500` | 114 | 0 | `5069e062` | `5069e062` | **MATCH** — draw, turn-cap, turn 30 |
| `jackioh-fuzz-1000` | 119 | 0 | `b2118821` | `b2118821` | **MATCH** — draw, turn-cap, turn 30 |
| `e2e/artifacts/01-hotseat-full-game.json` | 29 | 0 | `b2a3f515` | (none stored) | p1 wins, hero-death |

**Negative control**: folding seed 1's log under `jackioh-fuzz-1-tampered` gives `73250c6f`, which
is not `0d0272ca`. The check can fail.

The fourth row is the load-bearing one: `b2a3f515` is exactly the literal `EXPECTED_HASH` written
down at `packages/cards/test/hotseat-replay.test.ts:63`. So the live Cypress artefact, the committed
vitest fixture and an independent fold in a process that ran none of the suite all agree — which is
independent confirmation of spec 01's second clause.

All three sampled fuzz games terminated at or under the 30 player-turn cap (28, 30, 30), with 0
throws and 0 replay mismatches across the sample. `POOL_EXCLUSIONS` is `[]`
(`packages/cards/test/fuzz.test.ts:138`), so the pool is not narrowed.

`packages/engine/test/replay.test.ts:11,15` folds 100 recorded games to identical hashes, meeting
B1.2's "≥ 100", with `:27` "a different seed gives a different hash" as its own negative control.

---

## Part A — re-verification, not a fresh run

Per the brief, Part A was not redone. The four prior reports
(`2026-09-17-part-a.md`, `-r2`, `-r3`, `-r4`) raise **84 findings** between them; a delegated pass
re-checked every one of them against today's `SPEC.md` (894 lines, revision 5 of 2026-09-17).

**Result: 81 resolved, 2 standing (A-1, A-2 above), 1 partial (folded into A-2).** Both original
BLOCKERs are genuinely dead — verified, not taken on trust:
- r1 A-1 (Transmogulate pool): §8 #83 at SPEC.md:464 and R35 at SPEC.md:761 both state
  `{52, 85, 87, 92, 93, 95}`, which is exactly what I recomputed from §8's rarity column.
- r1 A-2 (unbounded cast-on-draw loop): `CAST_ON_DRAW_CHAIN_CAP` at §2.4 SPEC.md:63, R58
  SPEC.md:784, library cap R80 SPEC.md:806.

The four mechanical invariants were recomputed from today's file, not inherited:

| Check | Recomputed | Claim in SPEC | Result |
| --- | --- | --- | --- |
| A2.2 counts | §8 non-token rows = 100 (indices 1–100, no gaps, no duplicates); §8 token rows = 5 (51.1, 65.1, 90.1, 93.1, 95.1); §7 = 9 rows | §1 SPEC.md:7 "100 Core cards and 9 tokens"; §7 SPEC.md:330 "Nine token definitions" | **pass** (5 card-defined + 4 shared = 9) |
| A2.2 rarity | Common 35, Rare 37, Epic 16, Legendary 7, Mythic 5 = 100 | SPEC.md:359 "35 Common, 37 Rare, 16 Epic, 7 Legendary, 5 Mythic" | **pass** |
| A2.3 rulings | R1–R168, zero gaps, zero duplicates; decide rows = {R1, R2, R4, R5, R14, R26, R39} | §11 intro SPEC.md:723 lists exactly those seven as still open | **pass** on structure; the intro's *provenance* sentence is wrong → A-2 |
| A2.1 cross-refs | 53 headings, 168 R-ids; every `§N`, `§N.N`, `section N`, `(N.N)`, `R<n>` diffed mechanically | — | **pass**, 0 unresolved. Also 0 dangling `#N` card references anywhere in the file. |
| A2.5 pools | KY-tagged non-token = {31, 51, 57, 82}; Traps = {18, 41, 60, 71, 85, 96}, **all cost 1**; Legendary non-token = {52, 83, 85, 87, 92, 93, 95} | #57 SPEC.md:432, #67 SPEC.md:443, #83 SPEC.md:464, R35 SPEC.md:761 | **pass**, all three sentences reproduce exactly |

A2.4 (pipeline order), A2.6 (tokens), A2.7–A2.9 and A3.1/A3.2/A3.5 were re-read and spot-verified.
**A1.1, A1.2, A1.3, A1.4, A1.5, A3.3, A3.4 and A3.6 were not re-checked** — see "Not verified".

---

## Coverage of checks

| Check | Result | Evidence |
| --- | --- | --- |
| A1.1–A1.5 | **not re-checked** | Only the specific terms and sections prior rounds flagged (Flicker SPEC.md:326, Counter SPEC.md:297, §9.8, §9.4:535, §9.4:532, §9.5:540) were re-confirmed. |
| A2.1 | pass | Mechanical sweep: 0 unresolved references of any form. |
| A2.2 | pass | Recounted: 100 / 9 / 35-37-16-7-5. |
| A2.3 | pass (structure); A-2 (intro) | R1–R168, no gaps, decide rows match. |
| A2.4 | pass | §4.4 SPEC.md:157-172, steps 1–10 intact with the R63 zero rule, Trample cap and Cleave guard. |
| A2.5 | pass | All three pools recomputed and matched. |
| A2.6–A2.9 | pass | Re-read; §2.2:33-51, §6.2:274-275, §10.3:669 and R62:788 carry the identical turn sequence. |
| A3.1, A3.2, A3.5 | pass | §6.3 SPEC.md:286-326 (every verb r1 A-13 listed now has a row); §10.6:694-696; §10.1:590-645 with 0 hits for the four stale field names. |
| A3.3, A3.4, A3.6 | **not re-checked** | Only the cards prior findings named. |
| B0 | **not run** | Reported measurements relied on; see "Not verified". |
| B1.2 | pass | `packages/engine/test/replay.test.ts:11,15` — 100 recorded games to identical hashes. |
| B1.5 | pass | `Card.tsx:25-28` is a Set lookup over `legalActions`' output; REVIEW's grep false-positives. |
| B5 (`__jackioh` in the build) | partial | The current `apps/web/dist` is the `--mode development` e2e build: **7** occurrences of `__jackioh`, matching CI's claim. The source guard is real (`main.tsx:33`, `net.ts:62`, `routes/dev/hotseat.tsx:28` — `import.meta.env.MODE !== "production"`). The production bundle was not built; see "Not verified". |
| **B7** | **FAIL** (row 09), partial (row 10), pass (other ten) | Table above. `grep -rn "cy.wait([0-9]" e2e/` → nothing; zero `cy.wait(…)` of any form; enforced at run time by `e2e/support/e2e.ts:7-22`. All twelve specs exist; 33 `it`s at HEAD. |
| **B8** | **pass** | Three fuzz logs + the e2e recording folded in fresh processes; all hashes match; negative control differs. |
| M8 gate ("all twelve green in CI on Chrome and Electron") | pass, as reported | `.github/workflows/ci.yml` has a `matrix: browser: [chrome, electron]` with `fail-fast: false`, so the two are independent results. Built with `build:e2e`, served by `vite preview`, with a fresh `E2E=1` server per browser job (R144). |
| e2e deck fixtures | pass | `node e2e/scripts/check-fixtures.mjs` → "28 deck fixture(s) OK", exit 0. Fixtures are named for their spec, as the BUILD M8 preamble requires. |

---

## Not verified

1. **B0 in full.** I did not run `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`,
   `pnpm test:coverage`, `pnpm fuzz` or `pnpm test:e2e` — the brief forbids the browser suite, the
   whole vitest suite, the fuzz gate and the coverage run while other agents hold them. I relied on
   the reported measurements at 7c476c9: both browsers green, 12 spec files / 33 tests each
   (Electron 146 s, Chrome 153 s); vitest 3062 passed / 3 expected-fail / 9 todo over 214 files;
   lint 0; typecheck 0. The 33 is the one number I could corroborate statically, and it matches.
2. **Whether any spec consumed its one permitted retry.** `e2e/cypress.config.ts:31` sets
   `retries: { runMode: 1 }`, and the config's own comment says "treat a spec that needs the retry
   as a bug report". A spec that failed once and passed on the retry reports green, and the
   summary I was given carries pass counts only. Nothing in CI surfaces a consumed retry.
3. **The full fuzz gate's printed counts** (1,000 seeds, 0 throws, 0 hash mismatches, 0 over the
   cap). I sampled 3 seeds end to end; the other 997 are the CI agent's to report.
4. **The production bundle's `__jackioh` count.** Checking it means `pnpm build`, which would
   overwrite the `apps/web/dist` the browser agent is currently serving. Not run.
5. **Spec 09's in-flight rewrite.** Recorded at HEAD and graded there. The uncommitted working tree
   adds two `it`s (35 total) that do use the real screen and `cy.dragCardToDeck` — but only L2's
   message is asserted on screen; L1, L3, L5 and L6 remain JSON-only there too. Not graded.
6. **Part A, checks A1.1–A1.5 and A3.3/A3.4/A3.6.** Not re-walked. Two gaps are material and nobody
   has covered them:
   - **A1.3 has never been run against revision 5.** §8's internal shape is verified (105 rows,
     unique indices, correct rarity counts), but the card-by-card diff of cost / stats / text
     against `JackiOh_Core_Cards.md` was last done against revision 4. If revision 5 moved a
     number, no review has looked.
   - **R82–R168 have never been audited.** That is 87 rows, ~52% of §11, all added since r4. Their
     ids, decide-marking and card references check out; their **content** has not been read for
     contradictions against §2–§10 or against each other.
   Neither gap is a finding, but Part A's inherited PASS does not extend over either of them.
7. I modified nothing. The only writes were this report and scripts in the session scratchpad; the
   only command run inside `e2e/` was the read-only `node scripts/check-fixtures.mjs`.

---

## Verdict

**FAIL-MAJOR.** Two MAJORs (B-1, B-2), no BLOCKERs.

**BLOCKER ids: none.**

Neither MAJOR is a rule being wrong — the L1–L6 validator rules and the §9.4 invite gate are
implemented and proven at the validator, server and SQL layers. What is missing is the M8 acceptance
test at the layer BUILD M8's table names: spec 09 measures `PUT /api/loadout` where its row says
"shows" and "dragged", and spec 10 proves "code screen shown" as a URL rather than as a screen.
Under REVIEW §0 that is "a required test or acceptance item is absent", which is MAJOR, and
FAIL-MAJOR blocks the next gate — here, the definition of done.

The other ten rows are the strongest work in this repository's test suite. Specs 04, 05, 11 and 12
in particular compute their expected values from SPEC before the run, assert them as values rather
than as existence, and carry their own negative controls. The fixed-wait ban is enforced at run time
rather than documented. Spec 01's determinism claim survived an independent fold in a process that
ran none of the suite.

---

## Addendum — HEAD moved during this review

Everything above is graded at **7c476c9**, the commit named in the brief and the commit whose CI
results I was given. While this report was being written, two commits landed:

```
a30c3fd  Spec 09 tests the layer its row names: the drag, and the shown sentence
098c9b0  Give the 200-state walk a timeout chosen for it, not vitest's default
```

`a30c3fd` touches `09-deckbuilder.cy.ts` (+187/−15) and `06-room-code.cy.ts` (comment only). I have
**not run it** — the brief forbids Cypress — so nothing below is a pass claim. Statically:

- **B-5 is closed.** `06-room-code.cy.ts`'s stale R143 comment is now committed as corrected.
- **B-1 is partially closed, not closed.** Spec 09 grows to eleven `it`s.
  - Clause 2, "a card dragged into a second deck is refused": now measured on screen —
    `cy.dragCardToDeck` at `09-deckbuilder.cy.ts:479` and `:510`, the real multi-event gesture from
    `e2e/support/commands.ts:757-796`, with `loadoutErrorId("L4")` asserted absent at `:492`.
    This clause is addressed.
  - Clause 1, "each of **L1–L6** shows its message": still on screen for **L2 only**. Every
    `loadoutErrorId(...)` in the file names `"L2"` (`:437-451`, `:517`) or `"L4"` (`:492`, and that
    one is an absence). L1, L3, L5 and L6 remain asserted solely against `PUT /api/loadout` JSON
    through `expectRefusal`. One rule of six is shown; the row says six.
  - Clause 3, "save succeeds when legal": unchanged, still a `PUT` rather than a click on
    `LOADOUT_SAVE`.

So B-1 survives at reduced scope: two of its three clauses are now measured where BUILD's row puts
them, and "each of L1–L6 shows its message" is not. It stays **MAJOR** and the verdict stays
**FAIL-MAJOR** — which would also be the verdict on B-2 alone.

A re-review of M8 should start from `a30c3fd`, confirm the new specs go green on both browsers, and
check whether the remaining four rules are meant to be shown on screen or whether BUILD M8's row 09
should be rewritten to say what the suite is actually able to assert.

---

## Disposition — appended 2026-09-18 by the session that commissioned this review

This section is appended, not merged: the report above is the snapshot the reviewer produced and
stays as written. What follows is what happened to each finding, with the commit that closed it.
Nothing above has been edited to look better in hindsight.

| Id | Finding | Disposition |
| --- | --- | --- |
| B-1 | Spec 09 measures the layer beneath its row — no drag anywhere in the suite, "shows its message" untested | **Closed at reduced scope** — `a30c3fd`. The drag is now a real on-screen gesture with a control proving the gesture works (without it, "refused" and "never fired" leave an identical board). "Shows its message" is on screen for **L2 only**, and that is a property of the product, not a gap: `addCard` refuses any card already held in any deck, `poolFrom` never offers Tokens or unowned cards, and the builder always renders three decks, so five of the six rules cannot be provoked from a browser at all. The other five are rendered-message-tested in `Deckbuilder.test.tsx`, the only layer that can hand the builder an arbitrary draft. |
| B-2 | Spec 10's "code screen shown" is proved as a URL only | **Open, in progress.** This blocker is genuine — the testids really are absent from both the client and `testids.ts` — so the client must grow them first. Assigned. |
| B-3..B-9 | Minors | Recorded. `check-fixtures.mjs` still absent from CI; BUILD §4's nightly 20-seed spec-01 run still has no workflow; REVIEW.md's "fuzz artefacts" genuinely do not exist, which is a defect in REVIEW.md rather than in the code. |

The corroboration in §B8 of this report is the most valuable thing in it and is preserved here: three
fuzz logs folded in a process that ran none of the suite, with a negative control, and separately
`e2e/artifacts/01-hotseat-full-game.json` folded to **`b2a3f515`** — exactly the literal committed in
`packages/cards/test/hotseat-replay.test.ts`. The live Cypress artefact, the committed vitest fixture
and an independent fold all agree.

**Unresolved and worth carrying forward:** nothing surfaces whether a spec consumed its retry.
`retries.runMode` is 1, so a spec that failed once and passed reports green.
