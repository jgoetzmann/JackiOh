# JackiOh review — 2026-09-22 — feat/deck-library (M9)

Scope: Part B (milestone M9: the deck library, SPEC §11 R171–R173), by three independent read-only
referees run as separate sessions (CLAUDE.md rule 8), then a re-check of the failed referee's items
after the fixes, then a second fix round for what the re-check left open.
Verdict: **PASS** after fixes (first pass: Ref-Spec PASS, Ref-UX PASS, Ref-Bugs FAIL-MAJOR)
BLOCKER ids: **none**

| Referee | Lens | First verdict | Findings |
| --- | --- | --- | --- |
| Ref-Spec | REVIEW Part B in scope: B1 (client never enforces), B4 (rulings), B6 (server trust boundary), CLAUDE.md rules 3/7/9, BUILD M9 acceptance, docs | PASS | 6 MINOR |
| Ref-UX | The eight user requirements, driven in Chrome against the built client at 1440×900, 1440×790, 1920×1080 and 1920×970 | PASS | 2 MINOR |
| Ref-Bugs | Correctness and security of the diff, each finding reproduced with a scratch test | FAIL | 1 MAJOR, 3 MINOR |

---

## Findings

| ID | Severity | Location | Expected | Observed | Outcome |
| --- | --- | --- | --- | --- | --- |
| Bugs-F1 | MAJOR | `apps/web/src/game/library/Library.tsx` (save success) | Edits made while a save is in flight survive it | A landing save replaced the draft with the server's copy: the edit vanished, "Saved" showed, Back skipped the discard prompt | Fixed in a1eb2b3: the stored baseline and id are adopted, the current draft is kept, "Saved" only when clean. Test: "keeps edits made while a save is in flight, and the deck stays unsaved" |
| Bugs-F2 | MINOR | `Library.tsx` (discard confirm) | Discard is held while a save is in flight | Discard closed the editor and the landing save reopened it | Fixed in a1eb2b3 (Discard disabled while busy; a landing save only applies to the deck it was sent from). Test: "holds Discard while a save is in flight" |
| Bugs-F3 | MINOR | `packages/validator/src/index.ts` | Card ids are data: an unknown id is L6, never an exception | `"constructor"`, `"__proto__"` and friends read `Object.prototype` and threw; POST /api/decks and PUT /api/loadout answered 500 | Fixed in 81f555b (own-property lookups) with a validator test; a1eb2b3 also stops the decklist accepting any id the pages do not offer |
| Bugs-F4 | MINOR | `apps/server/src/api/http.ts` `readBody`; `packages/validator/src/index.ts` | A request cannot be amplified into a response many times its size | 538 KB of junk ids came back as an 11.8 MB 422 (also on /api/loadout). After round 1 the re-check found the residual inside the cap: a 64 KiB body still answered 2.4 MB (36×) | Round 1: new ruling R173 (360bf29) and 4e1031d cap every body at `MAX_REQUEST_BODY_BYTES` (64 KiB), 413 before parsing, counted in bytes, a lying Content-Length does not bypass it. Round 2: R173 extended so the validator judges an over-size deck by L2 alone and a loadout of too many decks by L1 alone; the re-check probe's worst case is now a 191-byte 422. Tests: `http.test.ts` and `validator.test.ts` "R173 …" |
| Spec-F1 | MINOR | `apps/web/src/routes/play.tsx` | CLAUDE.md rule 7: the client does not gate on a rule | A short library deck was disabled in the picker by `cards.length === DECK_SIZE` | Fixed in 81f555b: listed with its count and selectable; the server's L2 sentence answers the pick |
| Spec-F2 | MINOR | `apps/server/src/config.ts` `DECK_NAME_MAX_LENGTH` | A value citing R171 is stated by R171 | R171 did not mention the name bound | Fixed in 81f555b (R171 states it) |
| Spec-F3 | MINOR | `docs/architecture.md:22-52` | One table count | "13 tables" in the overview, 14 elsewhere; deck routes missing | Fixed in 81f555b |
| Spec-F4 | MINOR | `e2e/README.md:60`, `CLAUDE.md:63` | Spec 13 is listed among the specs that need the E2E server | Omitted | Fixed in 81f555b |
| Spec-F5 | MINOR | `packages/validator/test/validator.test.ts` | The R171 proof named for L5 asserts L5 | It asserted L3 and L6 only | Fixed in 81f555b |
| Spec-F6 | MINOR | `apps/server/test/api/decks.test.ts` | BUILD M9-T1: each route's refusals tested | PUT's 422 and pending 403 on PUT/DELETE were not exercised | Fixed in 4e1031d |
| UX-1 | MINOR | `apps/web/src/game/library/library.css` | Page cards readable without the inspector (requirement 7) | The four longest cards' text rendered at 6.8px at 1440×900 | Fixed in a1eb2b3: face rebalanced; smallest text 8.71px at 1440×900, 11.55px at 1920×1080, 0 cards clipped (all 100 measured) |
| UX-2 | MINOR | `library.css` | A full 20-card decklist fits at a normal desktop size | Overflowed the pane at 1440×900 (1 bar) and 1440×790 (4 bars) | Fixed in a1eb2b3: 27px bars + 3px gap; 0 hidden at 1440×900, 1440×790, 1920×1080, 1920×970 |
| Recheck-N1 | MINOR | `Library.tsx` save failure | A refusal is shown only for the deck it judged | A refusal landing after further edits was shown as the server's verdict on the edited deck | Fixed in round 2: the refusal is dropped when the deck changed since it was sent, as `edited` drops a verdict. Test: "drops a refusal that lands after the deck was edited" |
| Recheck-N2 | MINOR | `Deckbuilder.tsx` (loadout editor, also on main) | A drop from outside the page puts nothing in a deck | Dropped text such as "constructor" became a row | Fixed in round 2: only a card the pool offers goes in. Test: "puts in only a card the pool offers, whatever text is dropped" |

Round 1's re-check verdicts: F1 CLOSED, F2 CLOSED, F3 CLOSED (POST/PUT decks and PUT loadout answer 422 L6 for every prototype-named id; the Library ignores such drops), F4 PARTIAL (closed in round 2, above). Every round-2 test was mutation-checked: each fails with its fix removed.

## Coverage of checks

| Check | Result | Evidence |
| --- | --- | --- |
| B1 no validator sentence written by the client | pass | `messages.test.ts`, `validator-single-source.test.ts`, a diff-wide scan of the validator's template fragments |
| B1 rule 7 on /play | pass after Spec-F1 | picker no longer disables on a count |
| B4 rulings coverage | pass | `pnpm rulings:coverage`: 173 rows, R1–R173, every row named, no uncited R-id |
| B4 R171, R172 clause by clause | pass | Ref-Spec read every clause against `decks.ts`, `queue.ts`, `rooms.ts`, the validator and the loadout import |
| B6 no client write path to `decks` | pass | 0006: RLS, select-own policy, no insert/update/delete grant; `test:sql` CHECK 5 refuses a client INSERT |
| B6 isolation (routes and `deckId`) | pass | another profile's id is 404 on PUT/DELETE and at the queue and both room doors |
| B6 cap atomic | pass | Postgres profile-row lock; `test/db/contract.ts` "lands exactly one of two concurrent creates at one below the cap" (verified to fail with the lock removed) |
| B6 frozen decks | pass | queue and room "R172 editing or deleting the library deck after … does not change the match (§9.8)" |
| Rule 9 | pass | `MAX_LIBRARY_DECKS`, `DECK_NAME_MAX_LENGTH`, `MAX_REQUEST_BODY_BYTES` in `apps/server/src/config.ts`; deck shape from engine config |
| Requirement 1 (account-bound, ≤100) | pass | k/100 counter; decks survive reload; 100 created via API → New deck disabled, 101st POST 409 |
| Requirement 2 (20 unique, overridable) | pass | count stops at 20; no second copy; unfinished deck saves as Incomplete; `deckSize`/`maxCopies` props passed to the validator as `DeckRules` |
| Requirement 3 (pages, side arrows, 2–3 × 5–6) | pass | 5×3 at 1440×900 and 1440×790, 6×3 at 1920×1080, 6×2 at 1920×970; all cards fully in view; arrows disable at the ends |
| Requirement 4 (bars = cost + name) | pass | every bar's children are exactly cost gem and name |
| Requirement 5 (Hearthstone look) | pass | screenshots: book of cards with side chevrons, mana-gem bars on the right, count and Save at the bottom |
| Requirement 6 (hover preview) | pass | preview left of the list, level with the bar, inside the viewport, including the 20th bar |
| Requirement 7 (full-screen, 3D) | pass | overlay covers the viewport; card 62% of viewport height; real mouse drag rotates (`rotateX/rotateY`), flip shows the Radiant face, Esc closes |
| Requirement 8 (right-click add/remove) | pass | exactly that card added/removed; context menu suppressed only on cards and bars |
| Drag and drop | pass | page card → decklist adds; bar → pages removes |
| Loadout import, /play picker | pass | slot 2 filled with a copy; picker sends `{ deckId }` |
| Full gate | pass | lint, typecheck, validate:catalog, missing-tests, rulings:coverage (173 rows), 3297 tests, fuzz 1–1000, coverage 93.87% lines, `test:sql`, `test:db`; e2e 01–13 green on Chrome and Electron (99 skipped as designed); component spec green |

## Not verified

- Real OS-level HTML5 drag (the drag checks use synthetic `dragstart`/`dragover`/`drop`, as `dragCardToDeck` does; the inspector drag used real CDP mouse input).
- Firefox, touch input and `prefers-reduced-motion` in a real browser (reduced motion is covered in jsdom).
- The deployed stack (`99-online-smoke.cy.ts` is skipped unless enabled).
- Card text on non-macOS system fonts: the tightest card (Call to Chaos) has about 10% vertical headroom with the macOS font.
