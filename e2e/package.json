{
  "name": "@jackioh/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BUILD M8: the fourteen Cypress end-to-end specs. Runs against apps/web in E2E=1 mode.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "// typecheck:component": "The component specs are .tsx, which `tsconfig.json`'s `cypress/**/*.ts` glob does not match, so they have a project of their own rather than being silently unchecked.",
    "typecheck:component": "tsc -p cypress/component/tsconfig.json",
    "check:fixtures": "node scripts/check-fixtures.mjs",
    "test:e2e": "cypress run",
    "test:e2e:chrome": "cypress run --browser chrome",
    "test:e2e:electron": "cypress run --browser electron",
    "// test:component": "BUILD M5-T1's pixel acceptance on the full 10/10 fixture board. Needs no server and no client build: the Vite dev server in cypress.config.ts compiles apps/web/src directly.",
    "test:component": "cypress run --component",
    "test:component:chrome": "cypress run --component --browser chrome",
    "open": "cypress open",
    "open:component": "cypress open --component",
    "verify": "cypress verify"
  },
  "devDependencies": {
    "@types/node": "24.13.5",
    "@types/pg": "8.11.10",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@types/ws": "8.18.1",
    "@vitejs/plugin-react": "6.1.1",
    "cypress": "16.1.0",
    "pg": "8.23.0",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "typescript": "6.0.3",
    "vite": "8.3.0",
    "ws": "8.19.0"
  }
}
